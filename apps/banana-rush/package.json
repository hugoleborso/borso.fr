{
  "name": "@borso-app/banana-rush",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm dev:db > /dev/null && concurrently -k -n api,site -c yellow,green 'pnpm dev:api' 'pnpm dev:site'",
    "dev:db": "../../scripts/local-postgres.sh start banana-rush dev",
    "dev:api": "DATABASE_URL=$(../../scripts/local-postgres.sh start banana-rush dev) STAGE=dev tsx watch api/src/main.dev.ts",
    "dev:site": "vite",
    "db:local:start": "../../scripts/local-postgres.sh start banana-rush",
    "db:local:stop": "../../scripts/local-postgres.sh stop banana-rush",
    "db:local:wipe": "../../scripts/local-postgres.sh wipe banana-rush",
    "db:local:url": "../../scripts/local-postgres.sh url banana-rush",
    "build": "vite build && vite build --config vite.sw.config.ts",
    "preview": "vite preview",
    "lint": "cd ../.. && eslint apps/banana-rush --cache --cache-location node_modules/.cache/eslint",
    "typecheck": "tsc -p tsconfig.cdk.json --noEmit && tsc -p tsconfig.sw.json --noEmit && tsc --noEmit",
    "test:core": "pnpm --filter @borso/infra run build && vitest run --project core --coverage",
    "test": "DATABASE_URL=$(../../scripts/local-postgres.sh start banana-rush) vitest run --project back-e2e",
    "test:coverage": "pnpm --filter @borso/infra run build && DATABASE_URL=$(../../scripts/local-postgres.sh start banana-rush) vitest run --coverage",
    "db:generate": "drizzle-kit generate",
    "synth": "pnpm --filter @borso/infra run build && pnpm build && cdk synth --all",
    "diff": "pnpm --filter @borso/infra run build && pnpm build && cdk diff --all",
    "deploy": "pnpm --filter @borso/infra run build && pnpm build && cdk synth --all && ../../scripts/preflight-cloudfront-aliases.sh cdk.out && cdk deploy --all --require-approval never",
    "destroy": "pnpm --filter @borso/infra run build && pnpm build && cdk destroy \"banana-rush-pr-${PR_NUMBER:?PR_NUMBER env var required — banana-rush destroy targets the per-PR app stack only, never the shared banana-rush-cluster stack}\" --force",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "catalog:",
    "@aws-sdk/dsql-signer": "catalog:",
    "@borso/infra": "workspace:*",
    "@fontsource-variable/nunito": "5.2.5",
    "@hono/zod-validator": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@tanstack/react-form": "catalog:",
    "@tanstack/react-query": "catalog:",
    "class-variance-authority": "catalog:",
    "clsx": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "i18next": "catalog:",
    "postgres": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-i18next": "catalog:",
    "react-router-dom": "catalog:",
    "tailwindcss": "catalog:",
    "zod": "catalog:zod3"
  },
  "devDependencies": {
    "@hono/node-server": "catalog:",
    "@stryker-mutator/core": "catalog:",
    "@stryker-mutator/vitest-runner": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@types/ws": "8.5.14",
    "@vitejs/plugin-react": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "aws-cdk": "catalog:",
    "aws-cdk-lib": "catalog:",
    "concurrently": "catalog:",
    "constructs": "catalog:",
    "drizzle-kit": "catalog:",
    "jsdom": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "ws": "8.18.3"
  }
}
