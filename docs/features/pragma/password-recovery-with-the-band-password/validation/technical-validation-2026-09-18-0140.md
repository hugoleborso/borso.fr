# Technical validation — A member who forgot their password gets back in with the band password

- Spec: [`../spec/spec.md`](../spec/spec.md)
- Plan: [`../plan/plan.md`](../plan/plan.md)
- Branch: `claude/gracious-babbage-kmw2g6` at `4061138`
- Base: `origin/main`
- Run at: 2026-09-18T01:16:23Z
- Touched workspaces: `@borso-app/pragma` only (`apps/pragma/**`), plus this feature's documents under `docs/features/pragma/`. Nothing under `infra/**` changes.
- Supersedes: [`technical-validation-2026-09-18-0015.md`](./technical-validation-2026-09-18-0015.md) (FAIL), [`technical-validation-2026-09-18-0045.md`](./technical-validation-2026-09-18-0045.md) (FAIL) and [`technical-validation-2026-09-18-0115.md`](./technical-validation-2026-09-18-0115.md) (FAIL). Every row below was re-derived from the tree at `4061138` and from gates run in this session; no row was carried over on trust, and the two rows the earlier runs disagreed about — D06 and D12 — are re-argued from the files rather than from either report.
- Five use cases are routed to `/visual-validation` by the spec's *Test strategy* and are out of scope for this report: the *Forgot your password?* link being reachable from `/login`, the three fields accepting input, the refusal copy appearing on a wrong band password, the signed-in landing on `/catalog`, and `/enrol` no longer resolving in the browser. Their server-side halves are rows here; their browser halves are in [`visual-validation-2026-09-18-0015.md`](./visual-validation-2026-09-18-0015.md).

## A. Correctness vs spec

| # | Spec ref | Claim | Code (file:line) | Evidence (quoted) | Verdict |
|---|---|---|---|---|---|
| A01 | Result — one new endpoint | `POST /api/auth/recover-password`, public, body `{ username, sharedPassword, newPassword }` | apps/pragma/api/src/auth/auth.controller.ts:67, app.ts:38 | `.post('/recover-password', zValidator('json', recoverPasswordSchema), async (context) => {` on `publicRouter`, mounted by `.route('/api/auth', publicRouter)`; `requireMemberSession` is applied only to `rotateRouter` | PASS |
| A02 | Result — response shape | `200 { expiresAt, memberId }` | auth.controller.ts:84 | `return context.json({ expiresAt: outcome.session.expiresAt, memberId: outcome.memberId });` — identical to the login route's shape at :64 | PASS |
| A03 | Result — two endpoints deleted | `GET /api/auth/enrolment` and `POST /api/auth/enrol` are gone | auth.controller.ts diff | both handlers removed from `publicRouter`; no route string `enrol` survives anywhere in `apps/pragma/api/src` (`grep -rn "enrol" apps/pragma` returns only the 404 assertions, `createCredentialForMember`'s `already-enrolled` outcome and unrelated *passkey enrolment* names) | PASS |
| A04 | Q.O.D. 1 — typed username, no public listing | the offers endpoint and its service read are deleted; the username arrives in the body | credentials.schema.ts:58, credentials.service.ts diff | `export const recoverPasswordSchema = z.object({ username: usernameSchema, sharedPassword: passwordSchema, newPassword: passwordSchema });`; `readEnrolmentWindow`, `EnrolmentOffer` and `listCredentials` are removed, so nothing enumerates members publicly any more | PASS |
| A05 | Q.O.D. 2 — sign the member in on success | the recovery issues a session and writes the cookie | credentials.service.ts:153, auth.controller.ts:83 | `const session = await issueSession(credential.memberId, epoch, nowMillis);` then `writeSessionCookie(context, outcome.session);` — the same helper the login route uses | PASS |
| A06 | Q.O.D. 3 — passkeys survive | nothing in the recovery path touches `member_passkey` | credentials.service.ts:148-155 | the only write is `await updateCredentialSecret(credential.memberId, await hashPassword(params.newPassword), epoch);`; `insertPasskey` / `deletePasskey` are not reachable from `recoverPassword` | PASS |
| A07 | Q.O.D. 4 — dedicated stricter budget | `SHARED_PASSWORD_BUDGET`, 3 failures per 60 minutes, separate store from the login limiter | rate-limit.utils.ts:10-27, auth.controller.ts:44 | `export const SHARED_PASSWORD_MAX_ATTEMPTS = 3;` / `SHARED_PASSWORD_WINDOW_MS = SHARED_PASSWORD_WINDOW_MINUTES * SECONDS_PER_MINUTE * MILLISECONDS_PER_SECOND` with `SHARED_PASSWORD_WINDOW_MINUTES = 60`; the controller allocates `const sharedPasswordBucketStore = options.sharedPasswordBucketStore ?? createBucketStore();`, a second store, so sign-in failures cannot close the recovery door | PASS |
| A08 | Q.O.D. 5 — only one door onto the band password | the band password hash is read in exactly one place outside bootstrap/rotate | credentials.service.ts:143 | `const isSharedPasswordOk = await argon2Verify({ password: params.sharedPassword, hash: config.passwordHash });` is the only remaining read of `config.passwordHash` on a public route, and it sits behind the limiter at :138-140 | PASS |
| A09 | Q.O.D. 6 — enrolment page and endpoint deleted | page, form, core, queries, i18n keys and route all go | diff `--name-status` | `D apps/pragma/api/src/auth/enrolment.core.ts`, `D …/enrolment.core.test.ts`, `D apps/pragma/site/src/routes/EnrolPage.tsx`, `R061 EnrolForm.tsx → RecoverPasswordForm.tsx`; `useEnrol`, `useEnrolmentOffers` and `authKeys.enrolment()` removed from auth.queries.ts; every `auth.enrol*` key removed from both locales | PASS |
| A10 | Designer — dedicated `/recover` screen reached from a link under the sign-in form | route swapped, link swapped | App.tsx:33, LoginPage.tsx:68-69 | `<Route path="/recover" element={<RecoverPage />} />`; `<Link to="/recover" className="underline">{t('auth.recoverLink')}</Link>`, where `auth.recoverLink` is *"Forgot your password?"* / *"Mot de passe oublié ?"* | PASS |
| A11 | Designer — same `Card`, same width, same *Back to sign in* return | `RecoverPage` copies `EnrolPage`'s container | RecoverPage.tsx:22-45 | `<main className="min-h-dvh flex items-center justify-center bg-bg px-4 py-8">` / `<Card className="w-full max-w-[420px] p-6 sm:p-8">` / `<Link to="/login" …>{t('auth.backToLogin')}</Link>` — byte-identical container classes to the deleted `EnrolPage` (`R061` similarity on the form, container read side by side in `git show 9d01f0e`) | PASS |
| A12 | Use cases — the shared secret is verified before the username lookup | verify first, one refusal kind for both failures | credentials.service.ts:141-149 | `const isSharedPasswordOk = await argon2Verify({…}); if (!isSharedPasswordOk) return { kind: 'invalid-recovery' }; const credential = await findCredentialByUsername(params.username); if (credential === null) return { kind: 'invalid-recovery' };` — no branch between them distinguishes the two, and the controller maps the single kind to one body | PASS |
| A13 | Use cases — the epoch moves on every recovery | `nextSessionEpoch` is called in the same statement that writes the new hash | credentials.service.ts:150-151 | `const epoch = nextSessionEpoch(credential.sessionEpoch);` / `await updateCredentialSecret(credential.memberId, await hashPassword(params.newPassword), epoch);` — the same shape `changePassword` uses at :200-201 | PASS |
| A14 | Use cases — a new password equal to the old one is allowed | no branch compares the new password to the stored hash | credentials.service.ts:133-155 | the function body between the username lookup and the write contains no `argon2Verify` against `credential.passwordHash` and no equality test; the write is unconditional | PASS |
| A15 | Error cases — 401 `invalid-recovery`, 429 `rate-limited`, 503 `auth-not-bootstrapped` | the controller maps the three refusal kinds | auth.controller.ts:75-81 | `if (outcome.kind === 'rate-limited') return context.json({ error: 'rate-limited' }, 429);` / `return context.json({ error: 'auth-not-bootstrapped' }, 503);` / `return context.json({ error: 'invalid-recovery' }, 401);` | PASS |
| A16 | Error cases — a recovery-specific 429 copy that names an hour, not the sign-in copy | a distinct key with distinct text in both locales | login.core.ts:25-29, en.json, fr.json | `[TOO_MANY_ATTEMPTS_STATUS, 'auth.recoveryRateLimited'],` beside the login map's `[TOO_MANY_ATTEMPTS_STATUS, 'auth.rateLimited']`; `"recoveryRateLimited": "Too many attempts — try again in an hour."` / `"réessaie dans une heure."`, against the sign-in string's *a few minutes* / *quelques minutes*. This is what the spec asks for since `bd29370`; it was the `-0045` FAIL and stays closed | PASS |
| A17 | Error cases — a short password is refused before any hashing | validation runs in the route middleware, ahead of the handler | auth.controller.ts:67, credentials.schema.ts:51 | `zValidator('json', recoverPasswordSchema)` precedes the handler, and `passwordSchema = z.string().min(PASSWORD_MIN_LENGTH).max(PASSWORD_MAX_LENGTH)` with `PASSWORD_MIN_LENGTH = 8`; `hashPassword` is only reachable from the handler body | PASS |
| A18 | Changes — types | `RecoverPasswordOutcome` / `RecoverPasswordParams` as specified | credentials.service.ts:117-131 | the four outcome kinds and all six params match the spec's block field for field, including `readonly bucketStore: BucketStore` and `readonly now: Date` | PASS |
| A19 | Changes — database | no migration, no column | `apps/pragma/api/src/database/migrations` untouched | the diff contains no `.sql` file and no change to `memberCredentialTable`; `password_hash` and `session_epoch` already existed | PASS |
| A20 | Changes — files to change | every one of the enumerated entries appears in the diff in the stated direction | `git diff origin/main...HEAD --name-status` | `M` on `credentials.service.ts`, `credentials.schema.ts`, `rate-limit.utils.ts`, `rate-limit.utils.test.ts`, `auth.controller.ts`, `auth.controller.test.ts`, `test/auth-utils.ts`, `login.core.ts`, `login.core.test.ts`, `LoginPage.tsx`, `auth.queries.ts`, `App.tsx`, `en.json`, `fr.json`; `D` on `enrolment.core.ts`, `enrolment.core.test.ts`, `EnrolPage.tsx`; `A` on `RecoverPage.tsx`; `R061` on `EnrolForm.tsx → RecoverPasswordForm.tsx`. Four files change that the block does not list, all consequential: `credentials.repository.ts` (drops `listCredentials`, whose only caller was `readEnrolmentWindow`), `credentials.schema.test.ts`, `setlists/voting.controller.test.ts` and `VOCABULARY.md` | PASS |
| A21 | Changes — `test/auth-utils.ts` | `buildAuthenticatedApp` creates the credential directly and keeps its signature | test/auth-utils.ts:97-107 | `export async function buildAuthenticatedApp(firstName = 'Tester'): Promise<AuthenticatedApp> {` … `await giveMemberCredentials({ memberId, username });` / `const loginResponse = await loginAsMember(app, username);` — same parameter, same `AuthenticatedApp` return. Counted from the tree: 17 files besides `auth-utils.ts` reference the helper, the same 17 as on `origin/main`, and exactly one of them (`setlists/voting.controller.test.ts`) is in the diff, because it called the deleted `enrol()` helper directly. The spec's *Files to change* line 142 still says "its eighteen callers do not change" and is stale on both halves — see the note below; the code claim the row makes is met | PASS |
| A22 | Commit `4061138` claim 1 — a back-e2e case now recovers with the member's current password | the case exists, runs, and asserts all three halves | auth.controller.test.ts:200-214 | `it('accepts a new password equal to the old one and still moves the epoch', …)` — `const recovered = await recoverPassword(app, { newPassword: TEST_PASSWORD });` then `expect(recovered.status).toBe(200)`, the pre-recovery cookie on `/protected/ping` `toBe(401)`, and `loginAsMember(app, 'tester', TEST_PASSWORD, '203.0.113.51')` `toBe(200)`. Observed running: `✓ accepts a new password equal to the old one and still moves the epoch 1340ms` in the verbose back-e2e run | PASS |
| A23 | Commit `4061138` claim 2 — the spec's Developer line now says seventeen | the amended line is exact against the tree | spec.md:8 | "reaches the enrol endpoint on behalf of seventeen other test files … so sixteen of them stay untouched — the seventeenth called the deleted helper directly" — `grep -rl buildAuthenticatedApp apps/pragma` returns 18 paths, one of which is the defining file, and exactly one of the other 17 is in the diff | PASS |
| A24 | Plan — `useRecoverPassword` copies `useLogin` and carries no `onMutate` | pessimistic mutation, marker present | auth.queries.ts:74-82 | `// @FollowsBlueprint query-pessimistic-mutation` / `return useMutation({ mutationFn: postRecoverPassword, onSuccess: () => { rememberSessionMarker(); queryClient.setQueryData<SessionProbeResult>(authKeys.session(), { authenticated: true }); … } })` — no `onMutate`, no `invalidateQueries`, so `borso/no-refetch-of-optimistically-written-query` has nothing to say | PASS |
| A25 | Plan — BE↔FE types travel through `hc` | the call is a typed client call, no hand-rolled fetcher | auth.queries.ts:68 | `const response = await api.api.auth['recover-password'].$post({ json: variables });` | PASS |
| A26 | Plan — the architecture invariant holds for the new files | `architecture-graph.ts --check` green for `pragma` | scratch run | `pnpm exec tsx scripts/architecture/architecture-graph.ts --app pragma --check --out <scratch>` → exit 0, `pragma: 406 files across 4 levels and 19 slices.` (output redirected to scratch so the checkout is untouched) | PASS |
| A27 | Out of scope — `createCredentialForMember` survives, reachable only from test code | its callers are the test-seed route and the test helper | credentials.service.ts:161, test/auth-utils.ts:43 | `const { createCredentialForMember } = await import('../api/src/auth/credentials.service');` in `giveMemberCredentials`; the only other caller is `api/src/__test/test-seed.service.ts`. No public route reaches it | PASS |

## B. Code cleanliness

| # | Rule | Check | Evidence | Verdict |
|---|---|---|---|---|
| B01 | ESLint clean | `pnpm exec eslint --no-warn-ignored --max-warnings 0 apps/pragma` | exit 0; the single line of output is tsconfig-projects advice from typescript-eslint, not a rule result | PASS |
| B02 | Typecheck clean | `pnpm --filter @borso-app/pragma typecheck` | exit 0 (`tsc -p tsconfig.cdk.json --noEmit && tsc --noEmit`) | PASS |
| B03 | Build clean | `pnpm --filter @borso-app/pragma run build` | exit 0; the only warning is the pre-existing 500 kB chunk-size advisory on the site bundle | PASS |
| B04 | knip clean — the gate the plan calls load-bearing for a deletion | `pnpm exec knip` | exit 0; six configuration hints, zero unused files, exports or dependencies. `EnrolmentOffer`, `useEnrol`, `useEnrolmentOffers`, `authKeys.enrolment`, `listCredentials`, `selectEnrolErrorMessageKey` and the `enrolment.core` exports left together with their callers | PASS |
| B05 | No `any` | `grep -nP '\bany\b'` over the 18 changed `.ts`/`.tsx` files | one hit, in a test title: `it('falls back to the unknown message for any other status', …)` | PASS |
| B06 | Type assertions limited to `as const` / `as unknown` | `grep -nE '\bas [A-Za-z]'` minus those two forms | one hit, in a test title: `it('reads a setlist that was never voted on as locked', …)`. No assertion in changed source | PASS |
| B07 | No comments — annotations only | `grep -nE '^\s*(//|/\*)'` minus annotation lines | one hit: the `@Blueprint` JSDoc block on `buildAuthRouter` (auth.controller.ts:36-41), a machine-read annotation the rule keeps. The changed body of that block now says "login, password recovery and the passkey challenge stay open", which matches the routers it describes | PASS |
| B08 | `useEffect` is a smell | `grep -nE '\buseEffect\('` over changed `.ts`/`.tsx` | none found. `RecoverPage` holds its server error in `useState` set from the submit handler's `catch`, and navigation happens in the handler, not in an effect | PASS |
| B09 | Names carry intent, no one-letter locals | `grep -nE '\b(const\|let) [a-z] ='`, plus a read of the new files | none found. `sharedPasswordBucketStore`, `isSharedPasswordOk`, `credentialCounter`, `giveMemberCredentials`, `olderCookie`, `selectRecoverErrorMessageKey` — each names the result rather than the mechanism | PASS |
| B10 | Magic numbers and strings get names | read of `rate-limit.utils.ts`, `login.core.ts`, `RecoverPasswordForm.tsx`, `auth.controller.test.ts` | `SHARED_PASSWORD_MAX_ATTEMPTS = 3`, `SHARED_PASSWORD_WINDOW_MINUTES = 60`, `PASSWORD_MIN_LENGTH = 8`, `TOO_MANY_ATTEMPTS_STATUS`, `WRONG_PASSWORD_STATUS`, `NOT_BOOTSTRAPPED_STATUS`, `SHARED_PASSWORD_MAX_FAILURES`, `WRONG_SHARED_PASSWORD`, `NEW_PASSWORD`. The plan's specific commitment — the budget's `3` and `60` become named constants — is met | PASS |
| B11 | `noUncheckedIndexedAccess` honoured | `git diff … \| grep '^+' \| grep -E '\['` | the added lines contain no array index read at all. `api.api.auth['recover-password']` is a known-key property access on the typed `hc` client; `max-w-[420px]` and `text-[32px]` are Tailwind arbitrary values; `Buffer.from([1, 2, 3])` is a literal | PASS |
| B12 | Blueprint annotations resolve and no marker detached | `pnpm exec tsx .claude/skills/blueprint/blueprint-indexing.ts --check` | exit 0 — "Scanned 1245 source files: 178 blueprint(s), 1156 follower(s). Annotations are complete and the index is up to date." The new markers `service-orchestration`, `route-detail-page`, `route-form`, `query-pessimistic-mutation` and `test-back-e2e` all name existing blueprints, and `blueprint-subjects.json` shows no drift | PASS |
| B13 | Styling is inline Tailwind, mobile first | read of `RecoverPage.tsx` and `RecoverPasswordForm.tsx` | no `.css` import; `px-4 py-8`, `w-full max-w-[420px] p-6 sm:p-8`, `text-[32px] sm:text-[40px]`, `flex flex-col gap-3` — stacked by default with `sm:` opt-ins. The 375 px rendering itself is `/visual-validation`'s row | PASS |
| B14 | Atomic design buckets | `RecoverPasswordForm.tsx` sits in `components/organisms/` and composes `atoms/Button`, `atoms/Input`, `molecules/PasswordField` | `import { Button } from '../atoms/Button';` / `import { PasswordField } from '../molecules/PasswordField';` — no sibling-to-sibling import inside `organisms/` | PASS |
| B15 | Forms go through `@tanstack/react-form` | RecoverPasswordForm.tsx:29 | `const form = useForm({ defaultValues: { username: '', sharedPassword: '', newPassword: '' }, onSubmit: async ({ value }) => {…} });` — no `useState` chain | PASS |
| B16 | A test helper reaching production code through a dynamic `import` | test/auth-utils.ts:43 | `const { createCredentialForMember } = await import('../api/src/auth/credentials.service');` mirrors the pre-existing `createMemberDirectly` at :27, which does the same for `insertMember`; the shape is the file's established convention, not a new one | PASS |

## C. Tests pass

| # | Workspace | Command | Exit | Verdict |
|---|---|---|---|---|
| C01 | @borso-app/pragma | `pnpm --filter @borso-app/pragma run test:core` | 0 — `Test Files 172 passed (172)`, `Tests 1745 passed (1745)`, coverage 100 % statements / branches / functions / lines with `perFile: true` over `**/*.core.ts`, `**/*.utils.ts`, `**/*.adapter.ts`, `api/src/**/*.schema.ts` | PASS |
| C02 | @borso-app/pragma | `cd apps/pragma && pnpm run test` (back-e2e, own sandbox Postgres) | 0 — `Test Files 21 passed (21)`, `Tests 160 passed (160)`, 152 s | PASS |
| C03 | @borso-app/pragma | `pnpm exec vitest run --project back-e2e api/src/auth/auth.controller.test.ts --reporter=verbose` | 0 — 19 cases named, including `✓ accepts a new password equal to the old one and still moves the epoch 1340ms`. Run to prove the new case is collected and green, which the default reporter's counts cannot show | PASS |
| C04 | @borso-app/pragma | `pnpm exec stryker run --mutate api/src/auth/rate-limit.utils.ts,site/src/routes/login.core.ts` | 0 — 37 mutants, 37 killed, 0 survived, score 100.00 against a break threshold of 100 | PASS |
| C05 | @borso-app/pragma | `pnpm exec vitest run --project core site/src/routes/vote/VotePage.test.tsx` ×3 | 0, 0, 0 — `Tests 7 passed (7)` each time. This is the `-0015` FAIL: the retract case raced on a single `flushMicrotasks()`. `27e69ec` replaced both waits with `flushUntil(() => voteRow()?.getAttribute('aria-pressed') === …)`, which asserts the state it waits for, and three consecutive single-file runs plus the full `test:core` run are green | PASS |
| C06 | @borso-app/pragma | every `*.utils.ts` ships covered | the coverage configuration includes `api/src/**/*.utils.ts` and `site/src/**/*.utils.ts` with `thresholds.perFile`, so a utility with no test reports 0 % and breaks the run; C01 passed, therefore every one of them is at 100 %. The only `*.utils.ts` in the diff is `rate-limit.utils.ts`, whose sibling `rate-limit.utils.test.ts` is in the diff too. `test/auth-utils.ts` is test-support, not a `.utils.ts` module, and is outside the gate | PASS |
| C07 | — | no `infra/**` file changes, so `infra/cdk` and `infra/shared` coverage gates are not in scope | `git diff origin/main...HEAD --name-status` lists nothing under `infra/` | PASS |

## D. Test coverage of spec

Every item of the spec's *Use cases / edge cases* section, in order, minus the five routed to `/visual-validation`.

| # | Use case | Covering test | Verdict |
|---|---|---|---|
| D01 | Happy path 3-4 — the server verifies the band password, finds the credential, replaces the hash, and the response carries a session cookie | `it('replaces a forgotten password with the band password and signs the member in')` at auth.controller.test.ts:72 — `expect(recovered.status).toBe(200)`, `expect(setCookie).toMatch(/pragma_session=/)`, `/HttpOnly/i`, `/SameSite=Strict/i`, the new cookie opens `/protected/ping`, the new password then signs in and the old one returns 401 | PASS |
| D02 | Happy path 5 — every other session that member had open elsewhere is refused | `it('drops the sessions the member had open elsewhere when they recover')` at auth.controller.test.ts:92 — captures the pre-recovery cookie, recovers, `expect(olderBrowser.status).toBe(401)`. The 401 can only be `judgeMemberSession`'s `stale-epoch`: the credential row still exists and the cookie's TTL is 30 days | PASS |
| D03 | The shared secret is verified before the username lookup, so both failures answer with one message | `it('answers a wrong band password and an unknown username identically')` at auth.controller.test.ts:126 — `expect(unknownMember.status).toBe(wrongShared.status)` and `expect(await unknownMember.json()).toEqual(await wrongShared.json())`, each call from its own address so the limiter cannot confound them. The *equal argon2 cost* half is structural, not asserted: no test times the two paths, and nothing could without being flaky | PASS |
| D04 | Edge — the member's passkeys survive | `it('keeps the member passkeys alive through a recovery')` at auth.controller.test.ts:106 — inserts a passkey, recovers, `expect(passkeys).toHaveLength(1)`. The second half of Q.O.D. 3, that the device still authenticates, is structural and the plan says so: `finishPasskeyAuthentication` reads the epoch from the credential row at sign-in time | PASS |
| D05 | Edge — the username is typed with capitals or spaces | `it('reads a username back through the same trimming the sign-in form applies')` at auth.controller.test.ts:227 — recovers as `'  Borso  '` against a credential stored as `borso`, `expect(recovered.status).toBe(200)`, then signs in as `borso`; plus `it('lowercases and trims a username')` at credentials.schema.test.ts:13 | PASS |
| D06 | Edge — the new password may equal the old one: allowed, and the epoch still moves | `it('accepts a new password equal to the old one and still moves the epoch')` at auth.controller.test.ts:200 — the case added by `4061138`, and the `-0115` FAIL. It is discriminating on both halves: `expect(recovered.status).toBe(200)` fails against any rejection branch, since every refusal `recoverPassword` can express maps to 429, 503 or 401 at auth.controller.ts:75-81 and none of them is 200; and the pre-recovery cookie's `toBe(401)` fails against a short-circuit that skips the write when the password is unchanged, because the epoch is what the middleware compares. The *fresh salt* clause is structural: `hashPassword` passes `salt: randomBytes(ARGON2_SALT_BYTES)` on every call (credentials.service.ts:33), and no test reads the stored hash before and after | PASS |
| D07 | Edge — `app_config` holds no row, so the endpoint answers 503 | `it('answers 503 when the application was never bootstrapped')` at auth.controller.test.ts:216 — `expect(refused.status).toBe(503)` and `expect(await refused.json()).toEqual({ error: 'auth-not-bootstrapped' })` | PASS |
| D08 | Edge — a member exists but holds no credential: the same refusal as a wrong band password | Structural identity, covered by `it('answers a wrong band password and an unknown username identically')` at auth.controller.test.ts:126. The endpoint identifies nobody except through `findCredentialByUsername` (credentials.service.ts:148), so a member with no `member_credential` row has no username to type and is byte-identically the unknown-username input. There is no distinguishable second input to write a test for; the branch is the one the `'nobody'` case already drives | PASS |
| D09 | Error — wrong band password or unknown username → 401 `invalid-recovery`, rendered as *Wrong band password or name.* | auth.controller.test.ts:126 pins the 401 and the identical body, and `it('names the three failures the recovery flow plans for')` at login.core.test.ts:44 pins `expect(selectRecoverErrorMessageKey(401)).toBe('auth.invalidRecovery')`, whose `en.json` value is *"Wrong band password or name."*. The literal body code `invalid-recovery` is asserted only as "equal to the other refusal's body", never against its own string — no consumer reads it, since the front end maps on status alone | PASS |
| D10 | Error — more than three failed attempts from one address within an hour → 429 | `it('closes the recovery door after three failures from one address')` at auth.controller.test.ts:145 — three 401s, then `expect(blocked.status).toBe(429)`; and on the gated, mutation-covered pure file, `it('lets three failures through and refuses the fourth')` at rate-limit.utils.test.ts:118 with `it('opens a fresh window once the hour is past')` at :129. The budget's asymmetry is pinned independently at :109 by `toBeLessThan(MEMBER_LOGIN_BUDGET.maxAttempts)` and `toBeGreaterThan(MEMBER_LOGIN_BUDGET.windowMs)` | PASS |
| D11 | Error — a successful recovery clears the bucket, so a member who mistypes once and succeeds is not penalised | `it('clears the recovery budget once a recovery succeeds')` at auth.controller.test.ts:166 — one failure, one success, then three further failures all 401 rather than 429 | PASS |
| D12 | Error — a password shorter than eight characters is refused by `zValidator` before any hashing, 400 | `it('refuses a password shorter than eight characters')` at credentials.schema.test.ts:27 — `expect(passwordSchema.safeParse('short').success).toBe(false)` — on the exact schema object the recovery body's `newPassword` and `sharedPassword` are declared as (credentials.schema.ts:58-62). Two halves are *not* pinned and I am stating them rather than citing a test that does not reach them: nothing asserts that `recoverPasswordSchema.newPassword` is `passwordSchema` (`it('requires every field a recovery body carries')` at :39 only tests presence, and would stay green if the field were widened to `z.string()`), and no case asserts the 400 status on this route. The claim's substance is held by one statically readable line plus a test on the object it names; the composition is structural | PASS |
| D13 | Q.O.D. 4 / error cases — the recovery 429 copy is its own, not the sign-in one | `it('names the three failures the recovery flow plans for')` at login.core.test.ts:44 — `expect(selectRecoverErrorMessageKey(429)).toBe('auth.recoveryRateLimited')` — and `it('says an hour where the sign-in copy says a few minutes')` at :50 — `expect(selectRecoverErrorMessageKey(429)).not.toBe(selectLoginErrorMessageKey(429))`. Both are mutation-covered (C04). That the string *says* an hour is an i18n value, checked in the browser by `/visual-validation` (screenshot 14) and by reading `en.json` / `fr.json` in A16 | PASS |
| D14 | Q.O.D. 5 / 6 — the enrolment door is gone from the API | `it('no longer serves the enrolment endpoints')` at auth.controller.test.ts:241 — `expect((await app.request(…'/api/auth/enrolment')).status).toBe(404)` and the same for `POST /api/auth/enrol` with a well-formed body. The browser half — `/enrol` no longer resolving — is `/visual-validation`'s (screenshot 10) | PASS |
| D15 | Q.O.D. 4 — the budget is dedicated, so sign-in failures do not close the recovery door | `it('leaves a failed sign-in budget from counting against the recovery door')` at auth.controller.test.ts:186 — five failed sign-ins from one address, then a recovery from that same address returning 200. This is what the second `createBucketStore()` at auth.controller.ts:44 buys, and the only test that would notice if the two routes shared a store | PASS |
| D16 | Happy path 1-2 and 4 (browser halves), error copy rendering, `/enrol` not resolving | Routed to `/visual-validation` by the spec's *Test strategy*; see [`visual-validation-2026-09-18-0015.md`](./visual-validation-2026-09-18-0015.md). Not verified here, and not counted against this verdict | — |

## Notes

- **A21 — the spec's *Files to change* line 142 is stale, and `4061138` fixed the same sentence one line-number away.** It reads "`buildAuthenticatedApp` creates the credential directly; same signature, so its eighteen callers do not change", and both halves are wrong against the tree: the helper has seventeen callers, and one of them — `setlists/voting.controller.test.ts` — did change. The spec's own Developer line at :8 and the plan's row at :22 now state this correctly, which is why the row is PASS rather than FAIL: the test the `-0045` run set for itself was whether *anything in the feature folder describes what ships*, and two documents do. What is left is an internal contradiction inside one document, on a count, in an aside — a documentation defect worth one word in the next commit, not a reason to block a merge. `-0115` flagged the same line and asked for it to be fixed alongside the D06 test; that half of its recommendation was not taken. Fix: line 142 → "same signature, so sixteen of its seventeen callers do not change; the seventeenth called the deleted `enrol()` helper directly".
- **D12 is the weakest row in the report and the one I would strengthen next.** One case — `expect(recoverPasswordSchema.safeParse({ username: 'ada', sharedPassword: GOOD_PASSWORD, newPassword: 'short' }).success).toBe(false)` in `credentials.schema.test.ts` — would pin the composition the spec's error case depends on, and would cost one line. Nothing in the gates covers it today: `credentials.schema.ts` is coverage-included but a zod declaration is covered by being imported, and the mutation gate reaches only `*.utils.ts` and `*.core.ts`, so no mutant is ever generated for this file.
- **A02 has no test either.** The spec's *Result* block specifies `200 { expiresAt, memberId }`, and no back-e2e case reads the success body — the recovery tests assert the status and the cookie only, and the front end discards the parsed body. The contract is met in code and unpinned by an assertion. It is not a *Use cases* row, so it is not a D row, but it is the same shape of gap as D12 and the same one line of test.
- **On the discriminating power of the new D06 case, which is what this pass was asked to settle.** I could not demonstrate it by mutation: Stryker is scoped by the workspace's own gate to `rate-limit.utils.ts` and `login.core.ts`, `credentials.service.ts` is never mutated, and a validator that must not touch the working tree cannot introduce a rejection branch to watch the test go red. The argument from the code is nonetheless closed: `recoverPassword` returns one of four kinds; three map to 429 / 503 / 401 and the fourth to 200 (auth.controller.ts:75-84), so a fifth refusal kind for an unchanged password cannot answer 200 and `expect(recovered.status).toBe(200)` must fail. Independently, `expect(… /protected/ping …).toBe(401)` on a cookie minted before the recovery fails against any implementation that skips the `updateCredentialSecret` write when the password is unchanged, because `judgeMemberSession` compares the cookie's epoch with the credential row's. The third assertion — signing in afterwards with the same password — is the weakest of the three, since it would also hold if the recovery had done nothing at all; it carries meaning only next to the second.
- **`test:core` was red on the first run of this feature's validation (`-0015`) and is green now,** across the full suite and three consecutive single-file runs (C05). The fix is in the test, not in the product: `flushUntil` waits for the assertion instead of for one microtask turn.
- **Two friction entries were logged** to `KAIZEN.md` as `technical-validator-4`: a service-level test's discriminating power cannot be measured under a mutation gate scoped to `*.utils.ts` / `*.core.ts`, and proving a named back-e2e case ran costs a second 2.5-minute run because the default reporter prints counts only.

## Verdict: PASS
