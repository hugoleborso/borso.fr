# Technical validation — Lineup editor

- Spec: [`../spec/spec.md`](../spec/spec.md)
- Plan: [`../plan/plan.md`](../plan/plan.md)
- Branch: `claude/pragma-lineup-editor`
- Base: `main` (review window: commits `51729d8`..`a42402a` — the cascade + the lineup editor / per-member rollup; sibling commits on this branch ignored per operator brief)
- Run at: 2026-06-06T13:16:00Z
- Touched workspaces: `@borso-app/pragma`

> _N use cases routed to /visual-validation; out of scope for this report._ The spec's Test strategy delegates every UI happy/edge-path scenario (modal opens, badge appears/disappears, pill collapses, narrow-viewport scroll, empty state) to `/visual-validation`. The category D rows below cover only the pure-function / API contract assertions assigned here.

## A. Correctness vs spec

| #   | Spec ref                                                        | Claim                                                                                       | Code (file:line)                                                                                                                         | Evidence (quoted)                                                                                                                                                                                                                                                                          | Verdict |
| --- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| A01 | Q1 — edit surface on song + setlist-entry                       | Two trigger buttons opening the same molecule                                               | `apps/pragma/site/src/routes/catalog/SongDetailPage.tsx:253-262` + `apps/pragma/site/src/routes/setlists/SetlistEntryRow.tsx:291-298`    | Song surface: `<Button ... onClick={() => setLineupEditorOpen(true)}> ... {t('lineup.editDefault')}` ; Entry surface: `<button ... onClick={() => setLineupEditorOpen(true)} aria-label={t('lineup.edit')}> {t('lineup.edit')}`                                                            | PASS    |
| A02 | Q2 — unfiltered member picker                                   | Instrument `<select>` lists every instrument + "not playing"; no `member_instrument` filter | `apps/pragma/site/src/components/molecules/LineupEditor.tsx:165-170`                                                                     | `<option value={NOT_PLAYING_OPTION_VALUE}>{t('lineup.notPlaying')}</option> {instruments.map((instrument) => ( <option key={instrument.id} value={instrument.id}>{instrument.name}</option> ))}`                                                                                           | PASS    |
| A03 | Q3 — cascade scrub on member delete                             | Server-side scrub in same transaction as the member delete                                  | `apps/pragma/api/src/members/members.repository.ts:77-88`                                                                                | `return await database.transaction(async (tx) => { await scrubMemberFromSongDefaults(tx, id); await scrubMemberFromSetlistOverrides(tx, id); await tx.delete(memberInstrumentTable)... await tx.delete(memberTable)... })` — all scrubs + cascading deletes inside one Drizzle transaction | PASS    |
| A04 | Q4 — modal UX (native `<dialog>`)                               | `<dialog ref>` + `showModal()`                                                              | `apps/pragma/site/src/components/molecules/LineupEditor.tsx:103-106, 118-119`                                                            | `useEffect(() => { const dialog = dialogRef.current; if (dialog !== null && !dialog.open) dialog.showModal(); }, [])` + `<dialog ref={dialogRef} onClose={onClose} ...>`                                                                                                                   | PASS    |
| A05 | Q5 — no ADR trigger                                             | No new dep, no new secret, no schema migration                                              | `git diff 51729d8^..a42402a -- pnpm-lock.yaml apps/pragma/api/src/database/migrations/ apps/pragma/api/src/**/*.schema.ts` returns empty | (verified empty diff)                                                                                                                                                                                                                                                                      | PASS    |
| A06 | Use case 1 (song default)                                       | Save → `useUpdateSong` mutation                                                             | `apps/pragma/site/src/routes/catalog/SongDetailPage.tsx:89-92`                                                                           | `const handleSaveSongLineup = (lineup: LineupRecord \| null): void => { if (song === null) return; updateSong.mutate({ id: song.id, defaultLineup: lineup ?? {} }); };` — null collapsed to `{}` (song defaultLineup is non-nullable in schema)                                            | PASS    |
| A07 | Use case 2 (override entry lineup)                              | Save → `useUpdateSetlistEntry` with `lineupOverride`                                        | `apps/pragma/site/src/routes/setlists/SetlistEntryRow.tsx:114-116, 323`                                                                  | `const handleSaveLineup = (lineup: LineupRecord \| null): void => { props.onUpdate(props.entryId, { lineupOverride: lineup }); };` wired via `onSave={handleSaveLineup}`                                                                                                                   | PASS    |
| A08 | Use case 3 (reset override)                                     | `Reset` writes `lineupOverride: null` regardless of form state                              | `apps/pragma/site/src/routes/setlists/SetlistEntryRow.tsx:117-119` + `LineupEditor.tsx:108-112`                                          | `const handleResetLineup = (): void => { props.onUpdate(props.entryId, { lineupOverride: null }); };` ; Editor's Reset: `if (onReset === undefined) return; onReset(); onClose();`                                                                                                         | PASS    |
| A09 | Edge — empty default (all "not playing")                        | All-null selection collapses to `null` (override-badge binary on non-null per plan §11)     | `apps/pragma/site/src/components/molecules/LineupEditor.tsx:68-77`                                                                       | `function formValuesToLineup(values: FormValues): LineupRecord \| null { ... return hasAnyAssignment ? lineup : null; }` — emitted `null`, never `{}`                                                                                                                                      | PASS    |
| A10 | Edge — cascade on member delete                                 | Scrubs both song defaults and entry overrides                                               | `apps/pragma/api/src/members/members.repository.ts:90-125` + `apps/pragma/api/src/members/members.controller.test.ts:193-296`            | repository walks both tables, parses JSON with Zod schema, applies pure `scrubMemberFromLineup`, writes back; controller test asserts `expect(songAfter.song.defaultLineup).toEqual({ [keepMemberId]: guitarId })` and the entry's lineupOverride is similarly stripped                    | PASS    |
| A11 | Edge — same member assigned twice (impossible by construction)  | `Record<memberId, instrumentId \| null>` enforces one entry per member                      | `apps/pragma/site/src/components/molecules/LineupEditor.tsx:24`                                                                          | `export type LineupRecord = Readonly<Record<string, string \| null>>;` — keyed-by-member, structurally impossible                                                                                                                                                                          | PASS    |
| A12 | Error — optimistic-rollback path                                | Reuses existing TanStack mutation pattern                                                   | `apps/pragma/site/src/routes/setlists/SetlistEditor.tsx:170-172`                                                                         | `const handleUpdate = (entryId: string, patch: Record<string, unknown>): void => { updateEntry.mutate({ setlistId, entryId, ...patch }, { onError: recordError }); };` — onError surface present, mutation already wired with optimistic semantics in `queries/setlists.ts`                | PASS    |
| A13 | Use case 4 (read setlist as one member)                         | Sticky pill row + filtered entries + per-row instrument chip                                | `apps/pragma/site/src/routes/setlists/SetlistEditor.tsx:191-196, 240-291`                                                                | `<MemberFilterPills ... className="sticky top-0 z-10 bg-bg ..."/>` + `filtered = useMemo(() => filterEntriesForMember(entries, songsById, selectedMemberId), ...)` + per-row `prominentMemberInstrument={prominent}`                                                                       | PASS    |
| A14 | Use case 4 pill bar (one pill per member + "All members")       | `MemberFilterPills` molecule                                                                | `apps/pragma/site/src/components/molecules/MemberFilterPills.tsx:46-69`                                                                  | Leading All-members `<button ... onClick={() => onChange(null)}>{t('lineup.allMembers')}</button>` followed by `members.map(...)` per-member buttons                                                                                                                                       | PASS    |
| A15 | Edge — narrow phone (pill row scrolls horizontally)             | `overflow-x-auto whitespace-nowrap`                                                         | `apps/pragma/site/src/components/molecules/MemberFilterPills.tsx:42`                                                                     | `className={cn('flex gap-2 overflow-x-auto whitespace-nowrap py-2', className)}`                                                                                                                                                                                                           | PASS    |
| A16 | Edge — member with zero songs (empty state)                     | Empty-state copy renders                                                                    | `apps/pragma/site/src/routes/setlists/SetlistEditor.tsx:203-205`                                                                         | `{inFilteredMode && visibleEntries.length === 0 ? (<p className="...">{t('lineup.emptyForMember')}</p>) : (...)}` ; locale string `"Nothing to play tonight."` in en.json                                                                                                                  | PASS    |
| A17 | Override badge on entry row                                     | Renders when `lineupOverride !== null`                                                      | `apps/pragma/site/src/routes/setlists/SetlistEditor.tsx:288` + `SetlistEntryRow.tsx:162-166`                                             | Parent passes `hasOverride={entry.lineupOverride !== null}`; row renders `{props.hasOverride ? <span ...>{t('lineup.override')}</span> : null}` — binary on non-null, plan §11 honoured                                                                                                    | PASS    |
| A18 | i18n — new strings in both locales                              | en.json + fr.json keys added                                                                | `apps/pragma/site/src/i18n/en.json:224-241` + `fr.json:224-241`                                                                          | Both files carry the full `lineup.*` block (edit, editDefault, modal.title.song, modal.title.setlistEntry, notPlaying, resetToDefault, override, save, cancel, allMembers, emptyForMember, filterByMember)                                                                                 | PASS    |
| A19 | Pure helper — scrub                                             | `scrubMemberFromLineup(lineup, memberId)` returning new record                              | `apps/pragma/api/src/members/lineup-scrub.core.ts:12-22`                                                                                 | `export function scrubMemberFromLineup(...): Record<string, string \| null> { const scrubbed: Record<string, string \| null> = {}; for (...) { if (currentMemberId === memberId) continue; scrubbed[currentMemberId] = instrumentId; } return scrubbed; }` — pure, non-mutating            | PASS    |
| A20 | Pure helper — filter                                            | `filterEntriesForMember` returning `{ visibleEntries, instrumentByEntryId }`                | `apps/pragma/site/src/routes/setlists/setlist-filter.core.ts:26-43`                                                                      | matches spec signature exactly; null-selected pass-through returns every entry with an empty map                                                                                                                                                                                           | PASS    |
| A21 | Analytics events deferred (plan §1)                             | No emitter wired                                                                            | (no calls to `track\|analytics\|posthog\|amplitude` in pragma diff)                                                                      | matches plan's _Pre-implementation calls §1_ — intentional non-delivery                                                                                                                                                                                                                    | PASS    |
| A22 | Sentry tags deferred (plan §2); R1 detection via `console.warn` | `console.warn` floor present                                                                | `apps/pragma/site/src/routes/setlists/SetlistEditor.tsx:66`                                                                              | `console.warn({ surface: 'lineup-resolver', orphanMemberId, songId });` — structured payload matches plan's promised tag shape                                                                                                                                                             | PASS    |
| A23 | Override-badge binary on non-null (plan §3)                     | `{}` never persisted; reset writes `null`                                                   | `LineupEditor.tsx:76` (`hasAnyAssignment ? lineup : null`) + `SetlistEntryRow.tsx:118` (reset writes `lineupOverride: null`)             | matches plan §3 verbatim                                                                                                                                                                                                                                                                   | PASS    |
| A24 | `TransitionCommentModal` refactor stays out (plan §4)           | Native `<dialog>` only in `LineupEditor`; `TransitionCommentModal` untouched                | `git diff 51729d8^..a42402a -- apps/pragma/site/src/routes/setlists/TransitionCommentModal.tsx` empty                                    | intentional non-delivery                                                                                                                                                                                                                                                                   | PASS    |
| A25 | Files-to-change: `setlists.ts` query                            | Plan says "UPDATE if needed" — implementer assessed not needed                              | `git diff 51729d8^..a42402a -- apps/pragma/site/src/lib/queries/setlists.ts` empty                                                       | existing mutation already accepts `{lineupOverride: null}`; reset payload flows through cleanly                                                                                                                                                                                            | PASS    |

## B. Code cleanliness

| #   | Rule                                                                     | Check                                                                                                                                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Verdict |
| --- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| B01 | Names carry intent; no 1-letter locals                                   | grep on added lines for `(let \| const \| function) [a-z]\b`                                                                         | sweep shows `for (const [currentMemberId, instrumentId] of ...)`, `for (const orphanMemberId of ...)`, `for (const member of members)`; no `for (let i=...)` outside trivial scopes (e.g. transitions loop uses `index`, not `i`)                                                                                                                                                                                                                         | PASS    |
| B02 | Magic numbers / strings extracted to consts                              | sweep of changed files                                                                                                               | `lineup-scrub.core.ts` has no magics; `LineupEditor.tsx` declares `NOT_PLAYING_OPTION_VALUE = ''` and `FIELD_CLASS`; `MemberFilterPills.tsx` declares `PILL_BASE_CLASS`, `PILL_INACTIVE_CLASS`, `PILL_ACTIVE_CLASS`; `SetlistEditor.tsx` reuses pre-existing `ROW_HEIGHT_PX` / `ROW_GAP_PX` / `WARN_MARKER_OFFSET_PX`                                                                                                                                     | PASS    |
| B03 | Comments document WHY only (no what-, history-, "doesn't do X"-comments) | review every comment block in the diff                                                                                               | `lineup-scrub.core.ts` JSDoc explains "DSQL has no FK at write time, so the scrub is the only thing keeping the lineup-resolver from surfacing an orphan id" — WHY; `SetlistEditor.tsx:52-55` explains R1 detection floor + fire-once dedup — WHY; `setlist-editor.utils.ts:81-86` references R1 plan — WHY ; `client.ts:21-25` documents WHY a widened transaction-or-DB type exists — WHY ; no "previously this was X" or "doesn't do Y" comments found | PASS    |
| B04 | Function names describe the result                                       | review                                                                                                                               | `scrubMemberFromLineup`, `filterEntriesForMember`, `findOrphanMemberIds`, `prominentMemberInstrumentFor`, `resolveInstrumentForMember`, `scrubMemberFromSongDefaults`, `scrubMemberFromSetlistOverrides`, `lineupToFormValues`, `formValuesToLineup` — all describe the result, not the mechanism                                                                                                                                                         | PASS    |
| B05 | Type assertions limited to `as const` / `as unknown`                     | `git diff 51729d8^..a42402a -- 'apps/pragma/**/*.ts' 'apps/pragma/**/*.tsx' \| grep -E '^\+' \| grep -E '\\bas [A-Z]'` returns empty | one `as unknown` legitimately used in `members.repository.ts:95,116` for JSON.parse output → re-validated by `defaultLineupSchema.parse(lineupRaw)` immediately after; no chained `as Foo`                                                                                                                                                                                                                                                                | PASS    |
| B06 | No `any`                                                                 | `git diff ... \| grep -E '^\+' \| grep -E '\\bany\\b'` returns empty                                                                 | no `any` introduced                                                                                                                                                                                                                                                                                                                                                                                                                                       | PASS    |
| B07 | `noUncheckedIndexedAccess` honoured                                      | array-access guards present                                                                                                          | `transitions[fullIndex - 1]` returns `'safe' \| 'warn' \| undefined` and the `=== 'warn'` check is undefined-safe; `entries[gapIndex]` / `entries[gapIndex + 1]` checked with `if (... === undefined) return null;` ; in repository, `JSON.parse(...)` re-parsed through Zod                                                                                                                                                                              | PASS    |
| B08 | `DatabaseExecutor` widened type is not a disguised cast                  | `apps/pragma/api/src/database/client.ts:26`                                                                                          | `export type DatabaseExecutor = Parameters<Parameters<Database['transaction']>[0]>[0] \| Database;` — pure structural type derivation from Drizzle's transaction callback signature; no `as` involved                                                                                                                                                                                                                                                     | PASS    |
| B09 | `pnpm exec biome check` clean on changed files                           | ran biome check on all 16 touched files                                                                                              | `Checked 16 files in 1461ms. No fixes applied.` (exit 0)                                                                                                                                                                                                                                                                                                                                                                                                  | PASS    |
| B10 | `useEffect` is a smell — each one justified                              | only one new `useEffect` in this diff: `LineupEditor.tsx:103`                                                                        | mounts a native `<dialog>` via `dialog.showModal()`; this is a one-time imperative sync with an external system (the `<dialog>` DOM API has no declarative `open=true` for modal mode) — matches plan §70 ("only used to sync the `<dialog>` open state with the `open` prop")                                                                                                                                                                            | PASS    |
| B11 | Controller-as-dispatcher                                                 | `git diff ... -- apps/pragma/api/src/members/members.controller.ts apps/pragma/api/src/members/members.service.ts` returns empty     | scrub logic lives in `lineup-scrub.core.ts`, called from the repository inside the transaction; controller/service untouched                                                                                                                                                                                                                                                                                                                              | PASS    |
| B12 | Vertical-slice domain folders                                            | new files placed in correct bounded contexts                                                                                         | `lineup-scrub.core.ts` under `apps/pragma/api/src/members/` (member-context cascade); `setlist-filter.core.ts` under `apps/pragma/site/src/routes/setlists/` (setlist-page concern)                                                                                                                                                                                                                                                                       | PASS    |
| B13 | Atomic-design hierarchy                                                  | new components placed in `components/molecules/`                                                                                     | `LineupEditor.tsx` and `MemberFilterPills.tsx` both under `apps/pragma/site/src/components/molecules/` — alongside `MemberChip`, `CreateSessionDialog`, etc.                                                                                                                                                                                                                                                                                              | PASS    |
| B14 | Tailwind-only styling (no new `.css`)                                    | `git diff ... --stat -- '*.css'` returns empty                                                                                       | no new CSS files; all styling inline Tailwind utilities                                                                                                                                                                                                                                                                                                                                                                                                   | PASS    |
| B15 | BE↔FE via `hc<typeof apiRouter>`                                         | new mutations use existing typed client                                                                                              | `SongDetailPage` calls `updateSong.mutate(...)` (from `useUpdateSong` in `queries/songs.ts` which is built on `api.api.songs[':id'].$put`); `SetlistEntryRow` calls `props.onUpdate` which routes through `updateEntry.mutate` (from `useUpdateSetlistEntry`); no hand-rolled fetchers introduced                                                                                                                                                         | PASS    |
| B16 | `useQuery` / `useMutation` for server state                              | no `useState`+`useEffect(fetch)` patterns                                                                                            | all reads via `useSong`, `useMembersList`, `useInstrumentsList`, `useMasteryDefaults`, `useSetlistEntries`, `useSongsList`; all writes via existing mutation hooks                                                                                                                                                                                                                                                                                        | PASS    |
| B17 | Forms use `@tanstack/react-form`                                         | `LineupEditor.tsx` uses `useForm`                                                                                                    | `import { useForm } from '@tanstack/react-form'; ... const form = useForm({ defaultValues: lineupToFormValues(...), onSubmit: ... });` with `<form.Field name={member.id}>` per row                                                                                                                                                                                                                                                                       | PASS    |
| B18 | Mobile-first; pill row stays one line at 375 px                          | `whitespace-nowrap overflow-x-auto`                                                                                                  | `MemberFilterPills.tsx:42`: `flex gap-2 overflow-x-auto whitespace-nowrap py-2` + sticky wrapper in `SetlistEditor.tsx:195` uses `-mx-4 sm:-mx-9 px-4 sm:px-9` (mobile-first responsive padding)                                                                                                                                                                                                                                                          | PASS    |
| B19 | `pnpm exec knip` clean                                                   | ran knip                                                                                                                             | only two `Configuration hints` (pre-existing `ignoreDependencies` entries) — no unused exports / files reported on the changed code                                                                                                                                                                                                                                                                                                                       | PASS    |
| B20 | `pnpm --filter @borso-app/pragma typecheck` clean                        | ran typecheck                                                                                                                        | exit 0 (both `tsc -p tsconfig.cdk.json --noEmit` and `tsc --noEmit` succeed)                                                                                                                                                                                                                                                                                                                                                                              | PASS    |
| B21 | Orphan-warn `Set` does not leak unexpectedly                             | module-level dedup `Set` at `SetlistEditor.tsx:56`                                                                                   | `const warnedOrphanMemberIds = new Set<string>();` — persists for the lifetime of the page session, which is the intended fire-once-per-orphan semantic; tests do not exercise the warn path, so test-run isolation is moot; calling `warnIfOrphanMemberIds` during `visibleEntries.map(...)` is a side-effect-in-render but guarded by the Set and StrictMode-safe (the dedup absorbs the double-invoke)                                                 | PASS    |

## C. Tests pass

| #   | Workspace                           | Command                                                                                                                                                                                                            | Exit                                                  | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| C01 | @borso-app/pragma — core            | `pnpm --filter @borso-app/pragma exec vitest run --project core --coverage`                                                                                                                                        | 0                                                     | PASS (46 files, 399 tests; `lineup-scrub.core.ts`, `setlist-filter.core.ts`, `setlist-editor.utils.ts` all at 100/100/100/100)                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| C02 | @borso-app/pragma — back-e2e        | `pnpm --filter @borso-app/pragma run test`                                                                                                                                                                         | 0                                                     | PASS (13 files, 71 tests including the new cascade `members.controller.test.ts` case)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C03 | @borso-app/pragma — typecheck       | `pnpm --filter @borso-app/pragma run typecheck`                                                                                                                                                                    | 0                                                     | PASS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C04 | Biome                               | `pnpm exec biome check <16 changed files>`                                                                                                                                                                         | 0                                                     | PASS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C05 | Coverage threshold gate enforcement | `vitest.workspace.ts:32-42` declares `perFile: true` with 100% on stmts/branches/funcs/lines, with `include: ['api/src/**/*.core.ts', 'api/src/**/*.utils.ts', 'site/src/**/*.utils.ts', 'site/src/**/*.core.ts']` | gate IS configured to fail on <100% per file in scope | PASS — for the new files: `lineup-scrub.core.ts`, `setlist-filter.core.ts`, and `setlist-editor.utils.ts` all hit 100/100/100/100, so the gate is satisfied by every file this branch introduces or touches. (Observed: a handful of pre-existing `*.utils.ts` files — `embed.utils.ts`, `chordpro.utils.ts`, `member-color.utils.ts` — sit below 100% branches yet the run exits 0, suggesting the per-file branches threshold is not being enforced everywhere; this is a pre-existing config concern unrelated to the lineup-editor diff and surfaced here as an FYI only.) |

## D. Test coverage of spec

Spec's Test strategy assigns the following to /technical-validation: the two new `*.core.ts` files at 100% coverage and the back-e2e cascade assertion. Every UI behavioural assertion (modal open, badge appear/disappear, pill collapse, narrow viewport, empty state) is delegated to `/visual-validation` and not re-validated here.

| #   | Use case                                                                              | Covering test                                                                                                                                                | Verdict |
| --- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| D01 | `scrubMemberFromLineup` — empty lineup                                                | `apps/pragma/api/src/members/lineup-scrub.core.test.ts:12` — `it('returns an empty record when the input lineup is empty')`                                  | PASS    |
| D02 | `scrubMemberFromLineup` — target absent                                               | `lineup-scrub.core.test.ts:16` — `it('returns a copy unchanged when the target member is absent')`                                                           | PASS    |
| D03 | `scrubMemberFromLineup` — target present alone                                        | `lineup-scrub.core.test.ts:23` — `it('removes the target member when present alone')`                                                                        | PASS    |
| D04 | `scrubMemberFromLineup` — target in middle of multiple members                        | `lineup-scrub.core.test.ts:27` — `it('preserves the order of remaining members when the target sits in the middle')`                                         | PASS    |
| D05 | `scrubMemberFromLineup` — target with null instrument                                 | `lineup-scrub.core.test.ts:39` — `it('removes the target member even when their instrument is null (sitting out)')`                                          | PASS    |
| D06 | `filterEntriesForMember` — `selectedMemberId === null` pass-through                   | `apps/pragma/site/src/routes/setlists/setlist-filter.core.test.ts:29` — `it('returns every entry with an empty instrument map when no member is selected')`  | PASS    |
| D07 | `filterEntriesForMember` — member in zero entries                                     | `setlist-filter.core.test.ts:38` — `it('returns no entries when the selected member appears in zero lineups')`                                               | PASS    |
| D08 | `filterEntriesForMember` — member in N of M entries with correct per-entry instrument | `setlist-filter.core.test.ts:47` — `it('filters down to the entries where the member plays and reports their instrument')`                                   | PASS    |
| D09 | `filterEntriesForMember` — override wins over default                                 | `setlist-filter.core.test.ts:64` — `it('lets a lineup override take precedence over the song default when resolving')`                                       | PASS    |
| D10 | `filterEntriesForMember` — null in override drops entry                               | `setlist-filter.core.test.ts:75` — `it('drops an entry where the override sits the member out explicitly (null in override)')`                               | PASS    |
| D11 | `filterEntriesForMember` — null in default drops entry                                | `setlist-filter.core.test.ts:86` — `it('drops an entry where the song default sits the member out (null in default)')`                                       | PASS    |
| D12 | `filterEntriesForMember` — unknown song id drops entry                                | `setlist-filter.core.test.ts:97` — `it('drops an entry pointing to an unknown song id')`                                                                     | PASS    |
| D13 | Back-e2e — cascade on `DELETE /api/members/:id` clears both surfaces in one call      | `apps/pragma/api/src/members/members.controller.test.ts:194` — `it('scrubs the member from every song default lineup and setlist entry override on delete')` | PASS    |
| D14 | `LineupEditor` — opens with prefilled values                                          | `LineupEditor.test.tsx:121` — `it('prefills each select with the supplied lineup value')`                                                                    | PASS    |
| D15 | `LineupEditor` — Save returns full record + closes                                    | `LineupEditor.test.tsx:157` — `it('passes the edited lineup to onSave and closes the modal')`                                                                | PASS    |
| D16 | `LineupEditor` — all-not-playing collapses to `null` (plan §3 / §11 contract)         | `LineupEditor.test.tsx:185` — `it('collapses an all-not-playing selection to null on save ...')`                                                             | PASS    |
| D17 | `LineupEditor` — Reset fires `onReset` + `onClose` on setlist-entry surface           | `LineupEditor.test.tsx:211` — `it('shows the Reset button on the setlist-entry surface and calls onReset + onClose when clicked')`                           | PASS    |
| D18 | `LineupEditor` — Reset omitted on song surface                                        | `LineupEditor.test.tsx:234` — `it('omits the Reset button on the song surface')`                                                                             | PASS    |
| D19 | `LineupEditor` — Cancel does not call Save                                            | `LineupEditor.test.tsx:250` — `it('closes without saving when Cancel is clicked')`                                                                           | PASS    |
| D20 | `LineupEditor` — Save does not invoke `onReset` even when supplied                    | `LineupEditor.test.tsx:271` — `it('handles a Save click without invoking onReset even when supplied')`                                                       | PASS    |
| D21 | `MemberFilterPills` — one pill per member + "All members" pill                        | `MemberFilterPills.test.tsx:49` — `it('renders one pill per member plus an "All members" pill')`                                                             | PASS    |
| D22 | `MemberFilterPills` — clicking a member pill calls `onChange(memberId)`               | `MemberFilterPills.test.tsx:90` — `it('calls onChange with the member id when a member pill is clicked')`                                                    | PASS    |
| D23 | `MemberFilterPills` — clicking the All pill calls `onChange(null)`                    | `MemberFilterPills.test.tsx:102` — `it('calls onChange with null when the "All members" pill is clicked')`                                                   | PASS    |
| D24 | `MemberFilterPills` — active-pill aria-selected reflects `selectedMemberId`           | `MemberFilterPills.test.tsx:63, 74` — two cases covering `null` selection and member-id selection                                                            | PASS    |
| D25 | `findOrphanMemberIds` — orphan detection used by R1 console.warn                      | `apps/pragma/site/src/routes/setlists/setlist-editor.utils.test.ts:111-122` — three cases (every member known / orphan present / empty lineup)               | PASS    |
| D26 | `prominentMemberInstrumentFor` — null guard cases + happy path                        | `setlist-editor.utils.test.ts:126-153` — five cases (no instrument, no member selected, unknown member, unknown instrument, happy path)                      | PASS    |

## Notes

- _(C05)_ The vitest workspace gate is correctly declared to require 100% per-file across stmts/branches/funcs/lines. The new files (`lineup-scrub.core.ts`, `setlist-filter.core.ts`, `setlist-editor.utils.ts`) all hit 100% on every metric — gate is satisfied. The observation that some pre-existing `*.utils.ts` files run below 100% branches without failing the run is a pre-existing pragma test-infra concern, not an artefact of this branch; flagged as FYI only. The implementer's claim that coverage passes is verified for the files this branch touches.
- _(B10)_ The `useEffect` at `LineupEditor.tsx:103` is the only new effect in the diff and is genuinely legitimate: native `<dialog>.showModal()` has no declarative equivalent in React (the JSX `open` attribute opens a non-modal dialog only). Plan §70 explicitly authorises this single effect.
- _(B21)_ The orphan-warn module-level `Set` at `SetlistEditor.tsx:56` is correct in shape (fire-once-per-orphan, StrictMode-safe via the dedup). Calling `warnIfOrphanMemberIds` during the entries `.map(...)` is technically a side-effect-during-render, which is a yellow flag — but the operation is `console.warn` only (no state mutation), and the Set absorbs StrictMode's double-invoke. Acceptable. A future cleanup could move the orphan sweep into a separate `useMemo` over `entries+songsById+knownMemberIds` and call `console.warn` inside the memo body, but that is no cleaner than the current shape.
- _(A06)_ `SongDetailPage.handleSaveSongLineup` collapses `null` to `{}` before calling `updateSong.mutate({ id, defaultLineup: lineup ?? {} })`. This is correct: song `defaultLineup` is non-nullable in the BE schema (the override-null sentinel only applies to setlist entries). Verified by typecheck passing and by the existing `useUpdateSong` accepting a `defaultLineup: Record<string,string\|null>` field.
- _(A24, A25)_ Two intentional non-deliveries from the plan's _Pre-implementation calls_ section are confirmed: `TransitionCommentModal` left untouched (plan §4); `queries/setlists.ts` not modified (plan's "UPDATE if needed" — implementer assessed not needed and the existing mutation type accepts `{lineupOverride: null}` cleanly).

## Verdict: PASS
