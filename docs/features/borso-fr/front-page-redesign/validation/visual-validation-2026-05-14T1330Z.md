# Visual validation — Redesign de la front page borso.fr (react-bits Galaxy as React component, ADR 0003)

- Spec: [`../spec/spec.md`](../spec/spec.md)
- Dev URL: http://localhost:5173/
- Run at: 2026-05-14T13:30:00Z
- Tooling: agent-browser 0.27.0
- Scope: full re-validation after the architecture pivot from vendored vanilla WebGL (ADR 0002, superseded) to react-bits Galaxy mounted as a React component via `ogl` (ADR 0003). Visible output should be unchanged — same GLSL, same params — but the mount path is now `galaxy.tsx` → `createRoot(#bg-canvas-wrap).render(<Galaxy {...} />)`.

## Assertions

| # | From | Assertion | Action | Evidence | Verdict |
|---|---|---|---|---|---|
| 01 | Result / HP1 | Page loads at http://localhost:5173/ with no console error | Open URL, wait networkidle, read `agent-browser console --json` filtered to error/warning | `errors=[]` after full load + reload. Vite-only debug messages present. `./visual-validation-2026-05-14T1330Z/01-default-render.png` | PASS |
| 02 | Result / ADR 0003 | Galaxy React island mounts — `#bg-canvas-wrap` contains `.galaxy-container` with a `<canvas>` inside | DOM probe on initial render | `#bg-canvas-wrap > * = 1` child = `<div class="galaxy-container"><canvas width="1440" height="900" style="width:1440px;height:900px"></canvas></div>`. `galaxyContainer:true`, `canvasInWrap:true`. `./visual-validation-2026-05-14T1330Z/01-default-render.png` | PASS |
| 03 | Result | Title `borso.fr` centered, font Major Mono Display; "Bienvenue sur" above in Space Mono | Probe `.title` and `.welcome` text + computed `font-family` + bounding rect | `titleText:"borso.fr"`, `titleFont:"\"Major Mono Display\", ui-monospace, monospace"`, `welcomeText:"Bienvenue sur"`, `welcomeFont:"\"Space Mono\", ui-monospace, monospace"`. Title centerX=720 at viewport 1440 (perfectly centered). `./visual-validation-2026-05-14T1330Z/01-default-render.png` | PASS |
| 04 | HP / Q.O.D. (default closed) | Default-CLOSED menu: no `menu-open` on body, no `is-open` on `.menu`, burger SVG shows two lines, menu `aria-hidden=true` | DOM probe on initial render | `bodyClasses:"animate-in"` (no `menu-open`), `menuClass:"menu"` (no `is-open`), `menuAriaHidden:"true"`, `burgerAriaExpanded:"false"`, `burgerLines.display:"block"`, `burgerCross.display:"none"`. `./visual-validation-2026-05-14T1330Z/01-default-render.png` | PASS |
| 05 | HP4 / Result | Click burger → menu opens: body gains `menu-open`, `.menu.is-open`, .stage opacity 0.32 + scale 0.94, 5 links visible with staggered transition-delay (80 / 140 / 200 / 260 / 320 ms) | Click `@e2`, wait for transition, probe state + computed style of `.stage` + `<li>` inline `transitionDelay` + `<li>` opacity | After transition settles: `bodyClasses:"animate-in menu-open"`, `menuClass:"menu is-open"`, `stageOpacity:"0.32"`, `stageTransform:"matrix(0.94, 0, 0, 0.94, 0, 0)"`, `staggerDelays:["80ms","140ms","200ms","260ms","320ms"]`, `linkOpacities:[1,1,1,1,1]`. `./visual-validation-2026-05-14T1330Z/02-menu-open.png` | PASS |
| 06 | HP K2 | Escape closes the menu | From open state, `agent-browser press Escape`, re-probe | `bodyClasses:"animate-in"`, `menuClass:"menu"`, `menuAriaHidden:"true"` | PASS |
| 07 | HP K2 | Clicking burger again closes the menu and switches icon to lines | Click `@e2` twice (open then close), probe after each | After 2nd click: `bodyClasses:"animate-in"`, `menuClass:"menu"`, `burgerLines:"block"`, `burgerCross:"none"`. | PASS |
| 08 | HP5 / Result | 5 menu links with correct hrefs — Maman, Les sœurs, Art, Demande de date, Les 12 travaux de Borso | Probe `.menu a` href + textContent for each | hrefs: `family/mom.html`, `family/les-filles.html`, `art/mondrian/`, `mailto:hugo.borsoni@gmail.com?subject=Demande de date Nom Pr%C3%A9nom&body=Coucou tu m'as tap%C3%A9 dans l'oeil allons siroter un verre en terrasse`, `https://www.youtube.com/watch?v=dQw4w9WgXcQ`. Texts match in order. `./visual-validation-2026-05-14T1330Z/02-menu-open.png` | PASS |
| 09 | Edge / Mobile | Mobile (≤ 640 px): menu when open fills the screen, backdrop blur, 22 px font | Set viewport 360×640, reload, open menu, probe `.menu` rect + computed style | `rect={left:0, right:360, top:0, bottom:640, width:360, height:640}`, `fillsViewportX:true`, `fillsViewportY:true`, `position:"fixed"`, `backdropFilter:"blur(12px)"`, `background:"rgba(5,7,13,0.65)"`, `linkFontSize:"22px"`. `./visual-validation-2026-05-14T1330Z/05-mobile-menu-open.png` | PASS |
| 10 | Edge | `prefers-reduced-motion: reduce` — Galaxy frozen, title fade-up skipped | `agent-browser set media reduced-motion`, reload, probe + take two screenshots 3 s apart and compare MD5 | `prefersReducedMotion:true`, `bodyClasses:""` (no `animate-in`), `titleAnimation:"none"`, `titleOpacity:"1"`, `welcomeAnimation:"none"`, `welcomeOpacity:"1"`. Two screenshots 3 s apart have identical MD5 `47b5ee48ab4425980f378c618991cf92` (galaxy frozen). Sanity check after disabling reduced-motion: two screenshots 2 s apart have different MD5 (`58e48e3a…` vs `0e0ec3a8…`, galaxy animating). `./visual-validation-2026-05-14T1330Z/06-reduced-motion-t0.png`, `./visual-validation-2026-05-14T1330Z/06-reduced-motion-t3.png`, `./visual-validation-2026-05-14T1330Z/07-anim-t0.png`, `./visual-validation-2026-05-14T1330Z/07-anim-t2.png` | PASS |
| 11 | Result | WebGL canvas renders on desktop with non-zero size; mouse pointer can be moved over the canvas | Probe canvas size; `agent-browser mouse move` from (200,200) to (1200,700) and confirm canvas still renders | Canvas `width=1440 height=900 clientW=1440 clientH=900`. After two pointer moves the canvas still has a live WebGL context. (The `mouseRepulsion` effect is a GPU uniform — observable via screenshots changing between frames, which row 10's sanity check confirms.) `./visual-validation-2026-05-14T1330Z/03-mouse-moved.png` | PASS |
| 12 | Result | Grain overlay `#bg-grain` present with opacity 0.12, mix-blend overlay, z-index above the canvas wrap | Probe computed style on `#bg-grain`, `#bg-canvas-wrap`, `.stage` | `bgGrain.opacity:"0.12"`, `bgGrain.mixBlendMode:"overlay"`, `bgGrain.zIndex:"1"`. `bg-canvas-wrap.zIndex:"0"`, `.stage.zIndex:"2"`. Layer order = galaxy (0) < grain (1) < title (2). `transparent:false` on the Galaxy doesn't wash out — galaxy visible behind the dimmed (0.12) grain in screenshot 01. | PASS |
| 13 | Validator standard | Broken-image scan empty on every captured page | Run `Array.from(document.querySelectorAll('img')).filter((img) => img.complete && img.naturalWidth === 0)` after each screenshot | `[]` after every screenshot (no `<img>` tags exist; the burger icons are inline SVG, the grain is an inline SVG data-URI background-image). | PASS |
| 14 | New-architecture sanity | No reference to `#gradient-canvas`, `#nav-icon`, `#nav-menu`, `shader-bg.js` in served HTML | `curl http://localhost:5173/` and grep | grep returned no matches for any of the four legacy tokens. Selectors actually present: `bg-canvas-wrap`, `burger`, `menu`, `galaxy.tsx`, `script.js`. File `apps/borso-fr/site/shader-bg.js` does not exist on disk (deleted per ADR 0003). | PASS |
| 15 | React-island containment | `#bg-canvas-wrap` has at least one React-managed child (the `.galaxy-container` div), and the `<canvas>` is inside it | DOM probe | `#bg-canvas-wrap > * = 1`, innerHTML = `<div class="galaxy-container"><canvas …></canvas></div>`. React island is fully contained inside `#bg-canvas-wrap`; no React tree leaks into the `<main class="stage">` or `<nav id="menu">` blocks owned by the vanilla `script.js`. | PASS |

## Notes

> No FAIL or UNVERIFIABLE rows.

- The architecture pivot did not change any visible output: Major Mono Display title, dim+scale stage behaviour, stagger delays, mobile menu geometry, grain overlay layering, and broken-image hygiene all pass identically to the 2026-05-14T13:10Z run.
- Reduced-motion verification is stronger this run than the spec required: two screenshots taken 3 s apart under `prefers-reduced-motion: reduce` have byte-identical MD5 sums, proving the rAF loop is genuinely paused (the `disableAnimation` prop is wired correctly into the Galaxy component). The sanity-check pair under normal motion shows the expected frame-to-frame divergence.
- Console messages across the full session contained zero entries of type `error` or `warning` — only Vite HMR `[vite] connecting/connected` debug lines and the React DevTools info banner. No StrictMode double-render warnings, no `ogl` initialisation errors, no font-loading complaints.

## Verdict: PASS
