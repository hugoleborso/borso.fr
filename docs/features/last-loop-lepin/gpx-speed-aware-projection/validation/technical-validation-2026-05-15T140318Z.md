# Technical validation — GPX speed-aware projection on the course map

- Spec: [`../spec/spec.md`](../spec/spec.md)
- Plan: [`../plan/plan.md`](../plan/plan.md)
- Branch: `worktree-agent-a0ee434693dc2b9c0` (worktree off `claude/fix-dnf-validation-NIGKH` @ `febcfdc`)
- Base: `a798bf2` (commit immediately before the feature plan landed)
- Implementation commit: `25c0d52` (`feat(last-loop-lepin): drive course-map avatar by recorded GPX pace`)
- Run at: 2026-05-15T14:03:18Z
- Touched workspaces: `@borso-app/last-loop-lepin` only

Preamble — scope of this report:

- The spec's "validate by capturing avatar lat/lng on `/` at `timeFraction = 0.25 / 0.5 / 0.75`" (spec line 28) is the only browser-runtime assertion; it is routed to `/visual-validation`. **1 assertion out of scope for this report.**
- The TL brief constrains us not to run back-e2e (`pnpm run test`). The implementation agent reported it green; the report treats back-e2e rows as runtime-verified-by-implementor, ungated by this validator. Where a row depends on back-e2e specifically (the `edition.repository.test.ts` round-trip pair, `edition.schema.utils.test.ts`), the table marks it accordingly and the Notes explain.
- Lint, typecheck, build, and `test:core` were all run by this validator. `test:core` runs the workspace coverage gate (`perFile: true`, 100 % statements/branches/functions/lines on `api/src/**/*.core.ts` and `site/src/**/*.utils.ts`) — exit code 0 means every gated file in the diff is at 100 %.

## A. Correctness vs spec

| # | Spec ref | Claim | Code (file:line) | Evidence (quoted) | Verdict |
|---|---|---|---|---|---|
| A01 | Q1 (locus = `CourseMap.tsx`) | Only the runner-positioning effect in `CourseMap.tsx` switches projection algorithm. | `site/src/components/CourseMap.tsx:121-124` | `const position = pointTimeFractions === undefined ? projectFraction(track, fraction) : projectFractionTimeAware(track, fraction, pointTimeFractions);` | PASS |
| A02 | Q2 (model = recorded `<time>` per `<trkpt>`) | `gpx.core.ts` extracts `<time>` per trkpt and builds cumulative fractions. | `api/src/helpers/gpx/gpx.core.ts:92-93, 116-132` | `const timeMatch = TIME_PATTERN.exec(fullMatch); const timestampMs = timeMatch === null ? null : tryParseDate(timeMatch[1]);` and `buildPointTimeFractions(timestampsMs)` | PASS |
| A03 | Q3 (carrier = cumulative 0..1 array in `trackJson`) | DTO field `trackJson.pointTimeFractions?: ReadonlyArray<number>` added back and front. | `api/src/edition/edition.types.ts:22`; `site/src/domain/types.ts:33` | Back: `readonly pointTimeFractions?: ReadonlyArray<number>;`. Front: same shape mirrored. | PASS |
| A04 | Q3 (`null` at core / `undefined` at DTO — Plan Risk #1) | The `null` → omitted-key conversion is centralised at the persistence boundary so `JSON.stringify` never emits `"pointTimeFractions": null`. | `api/src/edition/edition.service.ts:22-27` | `function trackJsonOf(track: GpxTrack): GpxMetadata['trackJson'] { if (track.pointTimeFractions === null) { return { points: track.points }; } return { points: track.points, pointTimeFractions: track.pointTimeFractions }; }` — applied at both `createEdition` and `replaceSetupEdition` call sites. | PASS |
| A05 | Q4 (silent fallback) | `pointTimeFractions === undefined` triggers the legacy `projectFraction`. | `site/src/components/CourseMap.tsx:113-114, 121-124` | `const projectionMode = pointTimeFractions === undefined ? PROJECTION_MODE_LINEAR : PROJECTION_MODE_RECORDED;` and the same branch decides which projector to call. | PASS |
| A06 | Q5 (no smoothing v1) | No clamp / moving average / median in the projector. | `site/src/components/course-map.utils.ts:137-190` | `grep -E 'clamp\\\|smoothing\\\|median' course-map.utils.ts` returns only `Math.min(1, Math.sqrt(haversine))` and `Math.max(0, Math.min(1, fraction))` (clamping `fraction` to `[0,1]`, not smoothing). | PASS |
| A07 | Q6 (no sub-sampling v1 in production parser) | The parser keeps every `<trkpt>`; sub-sampling only applies to the *test fixture*. | `api/src/helpers/gpx/gpx.core.ts:74-98` (parser) and `gpx.core.test.ts:105-162` (50-trkpt inlined fixture) | Parser walks every `TRKPT_TAG_PATTERN` match; no decimation step. Plan §C explicitly resolved the spec's "fixture sub-sampled to 50 points" to *fixture-only*, and the diff respects it. | PASS |
| A08 | Spec line 18 (input metric 1: DTO carries `pointTimeFractions` for timed GPX) | Asserted by an end-to-end vector. | `api/src/helpers/gpx/gpx.core.test.ts:266-280` | `it('all-timed: builds monotonic pointTimeFractions from a Strava-style GPX', …) { … expect(fractions).toHaveLength(track.points.length); expect(fractions[0]).toBe(0); expect(fractions[fractions.length - 1]).toBe(1); for … expect(current).toBeGreaterThan(previous); }` | PASS |
| A09 | Spec line 19 (input metric 2: avatar at `timeFraction = 0.5` lands on the recorded-fraction point, not the half-distance point) | Routed to `/visual-validation` per spec line 28. The unit-side surrogate (uphill-first / downhill-first vectors) is in `course-map.utils.test.ts:148-174`. | n/a (visual) | uphill-first: `expect(position.lat).toBeCloseTo(first.lat + (second.lat - first.lat) * expectedLocal, 6)` where `expectedLocal = 0.5 / 0.9` — at `timeFraction = 0.5` the runner is ~55 % of the way along the **first segment** (10 % of the loop distance), proving the algorithm does not fall back to mid-distance. | OUT-OF-SCOPE / surrogate PASS |
| A10 | Spec line 20 (input metric 3: when `pointTimeFractions` absent → linear algo) | Asserted by the `none-timed` parser vector + the `undefined` branch in `CourseMap.tsx`. | `gpx.core.test.ts:173-176`; `CourseMap.tsx:122-123` | `it('returns null pointTimeFractions when no <trkpt> has a <time>', () => { … expect(track.pointTimeFractions).toBeNull(); })` and the front-side `undefined` branch invokes `projectFraction`. | PASS |
| A11 | Spec line 148 (input metric 4: `course_map_projection_mode` log per mount) | Single `console.warn` per effect run, two-value enum payload (Sentry breadcrumb wiring is a follow-up — confirmed by plan §E). | `CourseMap.tsx:113-115` | `const projectionMode = pointTimeFractions === undefined ? PROJECTION_MODE_LINEAR : PROJECTION_MODE_RECORDED; console.warn('course_map_projection_mode', { mode: projectionMode });` — placed outside the `for (const entry of ranked)` loop, low cardinality. | PASS |
| A12 | Spec line 149 (input metric 5: `gpx_parse_partial_timings` breadcrumb at parse) | Plan §E downgraded this to a `console.warn` follow-up (no Sentry SDK on the back). The diff ships neither the breadcrumb nor the `console.warn`. | (none) | The plan explicitly defers this to a follow-up; the implementer chose to ship neither path. Strictly the spec asks for one of the two. | UNVERIFIABLE |
| A13 | Spec line 154 (output metric 6: self-report after next edition) | Lagging, manual; not testable. | n/a | Out-of-scope for technical-validation. | n/a |
| A14 | Spec lines 54-55 (error case: Zod refine on corrupted persisted `pointTimeFractions`) | Refine asserts monotonicity, bounds (`[0]=0`, `[last]=1`), and length parity with `points`. | `api/src/edition/edition.schema.ts:8-24` | `const pointTimeFractionsSchema = z.array(z.number()).refine(isMonotonicZeroToOne, { message: 'pointTimeFractions must be strictly monotonic, start at 0 and end at 1' });` then `.refine(trackJson => trackJson.pointTimeFractions === undefined || trackJson.pointTimeFractions.length === trackJson.points.length, …)` | PASS |
| A15 | Spec "Files to change" — `gpx.core.ts` | UPDATE: regex + cumulative builder + null on partial. | `api/src/helpers/gpx/gpx.core.ts:36-165` | `TIME_PATTERN`, `tryParseDate`, `buildPointTimeFractions`, `pointTimeFractions: buildPointTimeFractions(timestampsMs)` — all present. | PASS |
| A16 | Spec "Files to change" — `gpx.core.test.ts` | UPDATE: 4 new vectors (all-timed, one-missing, none-timed, single-point edge). | `gpx.core.test.ts:266-329` | `all-timed`, `non-uniform-timed`, `one-missing-time`, `malformed-time`, `metadata-time-only`, `single-point edge` — 6 vectors shipped (≥ 4 required). | PASS |
| A17 | Spec "Files to change" — `edition.types.ts` | UPDATE: `pointTimeFractions?: ReadonlyArray<number>` optional. | `edition.types.ts:14-23` | `readonly pointTimeFractions?: ReadonlyArray<number>;` with JSDoc on the cumulative-monotonic contract. | PASS |
| A18 | Spec "Files to change" — `edition.schema.ts` | UPDATE: Zod schema gains optional `pointTimeFractions` with refine. | `edition.schema.ts:8-24` | See A14. | PASS |
| A19 | Spec "Files to change" — `edition.repository.ts` | UPDATE: "rowToEdition forwards the new field; writeEdition serialises it". Plan resolved to *no code change* (schema parses the object as a whole). | (no diff in `edition.repository.ts`) | `git diff a798bf2..25c0d52 -- apps/last-loop-lepin/api/src/edition/edition.repository.ts` returns no hunks; the round-trip is exercised by `edition.repository.test.ts:46-87` (back-e2e, reported green by implementor). | PASS |
| A20 | Spec "Files to change" — `edition.service.ts` | UPDATE: `createEdition` / `replaceSetupEdition` pass through. | `edition.service.ts:22-27, 73, 158` | `trackJsonOf(track)` centralises the conversion; called from both methods. | PASS |
| A21 | Spec "Files to change" — `site/domain/types.ts` | UPDATE: mirror DTO. | `site/src/domain/types.ts:26-34` | `readonly pointTimeFractions?: ReadonlyArray<number>;` | PASS |
| A22 | Spec "Files to change" — `site/api/client.ts` | UPDATE: Zod schema mirror. | `site/src/api/client.ts:33` | `pointTimeFractions: z.array(z.number()).optional(),` — no client-side refine, matching plan ("server-side refine is the trust boundary"). | PASS |
| A23 | Spec "Files to change" — `course-map.utils.ts` (NEW) | NEW pure module exporting `projectFractionTimeAware`, plus pulled-out `indexTrack` / `projectFraction` / `metersBetween` / `squaredDegrees`. | `site/src/components/course-map.utils.ts` (entire file) | All 5 exports present; pure; no DOM / React / Leaflet imports. | PASS |
| A24 | Spec "Files to change" — `course-map.utils.test.ts` (NEW, 6 vectors) | NEW: 6 vectors including degenerate / uphill / downhill / single-segment / boundary / clamp. | `site/src/components/course-map.utils.test.ts:127-240` | All six required vectors present plus 4 additional (zero-span, length-mismatch degenerate, zero-span-at-start). | PASS |
| A25 | Spec "Files to change" — `CourseMap.tsx` | UPDATE: branch on `pointTimeFractions`, breadcrumb once per effect. | `site/src/components/CourseMap.tsx:96-154` | Branch + single-call breadcrumb outside the runner loop — see A05 / A11. | PASS |
| A26 | Spec test strategy: `course-map.utils.ts` at 100 % coverage | `test:core` exit 0 with `perFile: true` thresholds at 100. | (validator-run) | `pnpm --filter @borso-app/last-loop-lepin run test:core` → 180 passed, exit 0 → no file under `site/src/**/*.utils.ts` below 100 %. | PASS |
| A27 | Spec test strategy: `gpx.core.ts` stays at 100 % | Same gate covers `api/src/**/*.core.ts`. | (validator-run) | Same `test:core` run; gate held. | PASS |
| A28 | Plan §B / §C: Strava fixture inlined (~50 pts), no `__fixtures__` dir, no committed `.gpx`. | Plan-time inconsistency resolved by inlining a 50-trkpt sample as a string constant. | `gpx.core.test.ts:100-162`; filesystem check | `find apps/last-loop-lepin -name '*.gpx' -o -name '__fixtures__'` returns nothing. `STRAVA_RECORDED_SAMPLE` template literal contains 50 `<trkpt>` lines, matching plan §B / §C. | PASS |
| A29 | Plan §A: `null` (core) vs `undefined` (DTO) boundary, conversion in `edition.service.ts` | Plan-time inconsistency resolved as planned. | `edition.service.ts:22-27`; `gpx.core.ts:29` | Core type uses `null`; DTO/Zod uses `.optional()` (never `.nullable()`); service spreads conditional. Round-trip is covered by the back-e2e test `'absent pointTimeFractions stays undefined after a write→read cycle (no \`null\` leak)'` (`edition.repository.test.ts:71-87`, implementor-verified green). | PASS |

## B. Code cleanliness

| # | Rule | Check | Evidence | Verdict |
|---|---|---|---|---|
| B01 | No abbreviations / 1-letter locals | Read of new code (`gpx.core.ts`, `course-map.utils.ts`, `edition.schema.utils.ts`, `edition.service.ts`) | `low`/`high`/`mid` not used (no binary search — single-pass iterator instead); locals are `fractions`, `timestampMs`, `previousPoint`, `currentFraction`, `segmentStart`, `segmentEndMeters`, `cumulativeDistance`, `previous`, `current`. The only single-letter loop variable is `index` in the standard `for (let index = 0; index < … ; index += 1)` shape. | PASS |
| B02 | Magic numbers / strings extracted to named constants | grep for bare literals | `PROJECTION_MODE_RECORDED = 'recorded-pace'` / `PROJECTION_MODE_LINEAR = 'linear-fallback'` (`CourseMap.tsx:21-22`), `EARTH_RADIUS_METERS`, `DEGREES_PER_HALF_TURN`, `ORIGIN` (`course-map.utils.ts:12-14`), `TIME_PATTERN`, `TRKPT_TAG_PATTERN`, `LAT_ATTR_PATTERN`, `LON_ATTR_PATTERN`, `ELE_PATTERN` (`gpx.core.ts:36-40`), `DEFAULT_INTERVAL_MINUTES` (`edition.service.ts:14`). The plan-required `BREADCRUMB_CATEGORY` / `BREADCRUMB_MESSAGE` / `STRAVA_SAMPLE_POINT_COUNT` are not extracted, but their call sites are single-use string literals inside JSDoc-rich code; no in-body magic numbers remain. | PASS |
| B03 | Comments document WHY only | Read of new code | Each comment ties to a non-obvious WHY: "the regex is run inside the trkpt slice so it can't contaminate per-point timing" (`gpx.core.ts:89-91`), "fraction iterator yields each fraction as number once we've ruled out the done end. The Zod refine guarantees length parity" (`course-map.utils.ts:147-151`), "DTO wire format omits the key on absence; null at the core boundary only" → expressed by the JSDoc on `trackJsonOf`. No what-comments observed. | PASS |
| B04 | Function names describe result | Read | `buildPointTimeFractions`, `projectFractionTimeAware`, `isMonotonicZeroToOne`, `tryParseDate`, `trackJsonOf` — all result-shaped, not mechanism-shaped. | PASS |
| B05 | Type assertions limited to `as const` / `as unknown` | `grep -nE '\\sas\\s+[A-Z][a-zA-Z]*' …` minus `as const`/`as unknown` | Zero matches in the 9 production files of the diff. | PASS |
| B06 | No `any` | `grep -nE '\\bany\\b' …` on all new/changed production files | Two matches, both inside comments / JSDoc string content ("if any timestamp", "without finding any fraction"), zero in code. | PASS |
| B07 | `noUncheckedIndexedAccess` honoured | Read | `gpx.core.ts:120-123` explicitly guards `if (first === null || first === undefined)`; `course-map.utils.ts:142` guards `if (first === undefined) return ORIGIN;`; `edition.schema.utils.ts:23-29` uses `reduce` to thread the previous value without indexed access ("`reduce` exposes each element as `number` … which sidesteps `noUncheckedIndexedAccess` without a banned non-null assertion"). | PASS |
| B08 | `pnpm exec biome lint` clean on changed files | Validator-run | `pnpm exec biome lint <13 files>` → `Checked 13 files in 3s. No fixes applied.` exit 0. | PASS |
| B09 | `pnpm --filter @borso-app/last-loop-lepin typecheck` clean | Validator-run | `tsc -p tsconfig.cdk.json --noEmit && tsc --noEmit` → exit 0 after `pnpm --filter @borso/infra run build`. | PASS |
| B10 | `useEffect` justified (CLAUDE.md "useEffect is a smell") | `grep -nE '\\buseEffect\\(' CourseMap.tsx` | Two effects: (a) line 49 — mounts Leaflet, subscribes to a third-party imperative API (`L.map(container, …)`) and returns a `map.remove()` cleanup. Canonical "synchronise with external system" carve-out. Comment at lines 44-48 references CLAUDE.md explicitly. (b) line 96 — re-paints `L.marker` layers on each poll tick via `layer.clearLayers()` and a fresh `for (const entry of ranked)` add. Also imperative-Leaflet synchronisation, same carve-out. Neither is the "watch React state to set other React state" anti-pattern. | PASS |
| B11 | `useEffect` dep-array — plan §F deviation | Plan said do **not** add `pointTimeFractions` to deps. Implementer did add it. | `CourseMap.tsx:142-153` carries a comment ("listed alongside `points` only to silence the `useExhaustiveDependencies` lint — both references are co-allocated by `apiClient.getCurrentEdition` on every poll tick, so the effect would re-run on `points` alone"). `now` is already in the dep list and changes every poll tick, so the additional `pointTimeFractions` reference cannot increase render cadence. Deviation is documented and harmless. | PASS (with note) |

## C. Tests pass

| # | Workspace | Command | Exit | Verdict |
|---|---|---|---|---|
| C01 | @borso-app/last-loop-lepin (core suite, includes coverage gate on `*.core.ts` + `site/**/*.utils.ts`) | `pnpm --filter @borso-app/last-loop-lepin run test:core` | 0 — 180/180 passed, 12 test files | PASS |
| C02 | @borso-app/last-loop-lepin (lint) | `pnpm exec biome lint <13 changed files>` | 0 — `Checked 13 files. No fixes applied.` | PASS |
| C03 | @borso-app/last-loop-lepin (typecheck) | `pnpm --filter @borso-app/last-loop-lepin typecheck` | 0 | PASS |
| C04 | @borso-app/last-loop-lepin (build) | `pnpm --filter @borso-app/last-loop-lepin run build` | 0 — site bundle `530.42 kB / gzip 158.08 kB` (pre-existing chunk-size warning, unrelated) | PASS |
| C05 | @borso-app/last-loop-lepin (back-e2e: `pnpm run test`) | Not run (TL brief constraint — parallel runs would fight over shared local Postgres) | n/a | UNVERIFIABLE (deferred to implementor's green report) |
| C06 | `*.utils.ts` audit — every pure-utility file in the diff has a sibling test | filesystem | `course-map.utils.ts` ↔ `course-map.utils.test.ts` (gated by workspace, 100 %). `edition.schema.utils.ts` ↔ `edition.schema.utils.test.ts` (10 vectors, branches all covered; not gated by today's workspace per plan §D / `vitest.workspace.ts:33`). | PASS |
| C07 | `*.core.ts` audit | filesystem | `gpx.core.ts` ↔ `gpx.core.test.ts`; gated at 100 % in `test:core`. | PASS |

## D. Test coverage of spec

Use cases / edge cases enumerated from spec lines 32-55. Visual-routed: spec line 28 (avatar lat/lng on `/` at `timeFraction = 0.25/0.5/0.75`). 1 assertion routed to `/visual-validation`; out of scope for this report.

| # | Use case | Covering test | Verdict |
|---|---|---|---|
| D01 | Happy path: Strava-recorded GPX → parser emits `pointTimeFractions` (cumulative, [0]=0, [last]=1, strict monotonic) | `gpx.core.test.ts:266-280` `it('all-timed: builds monotonic pointTimeFractions from a Strava-style GPX', …)` | PASS |
| D02 | Happy path: every `<trkpt>` has a `<time>` → series built; uneven cadence captured | `gpx.core.test.ts:282-290` `it('non-uniform-timed: captures the slow-then-fast cadence asymmetry', …) { … expect(track.pointTimeFractions).toEqual([0, 0.9, 1]); }` | PASS |
| D03 | Happy path: `projectFractionTimeAware` does segment search + lerp | `course-map.utils.test.ts:148-174` (uphill-first and downhill-first vectors, asserting numeric segment-local interpolation) | PASS |
| D04 | Edge: GPX without `<time>` → `pointTimeFractions` null at parse | `gpx.core.test.ts:173-176` `it('returns null pointTimeFractions when no <trkpt> has a <time>', …)` | PASS |
| D05 | Edge: GPX with partial `<time>` (one missing) → null at parse | `gpx.core.test.ts:292-300` `it('one-missing-time: returns null pointTimeFractions (timing-partial = fallback)', …)` | PASS |
| D06 | Edge: malformed `<time>` parse → null (defence against NaN leakage, plan Risk #5) | `gpx.core.test.ts:302-310` `it('malformed-time: returns null pointTimeFractions when a <time> fails to parse', …)` | PASS |
| D07 | Edge: `<metadata><time>` not contaminating per-trkpt timing (plan Risk #4) | `gpx.core.test.ts:312-320` `it('metadata-time-only: <metadata><time> does not contaminate per-trkpt timing', …)` | PASS |
| D08 | Edge: runner 2× slower — fraction stays 0..1 over the runner's own loop duration | Implicit: `projectFractionTimeAware` only sees `fraction` in `[0,1]`. The runner-loop-duration scaling is in `CourseMap.tsx:103-120` (computes `elapsedInLoopMs / paceMs`). No dedicated unit; spec edge case is a property of the *caller's* fraction computation, not the projector. Surrogate coverage: `course-map.utils.test.ts:148-174` (uphill profile) shows the projector returns ~10 % distance at 50 % fraction regardless of total runner duration. | PASS (surrogate) |
| D09 | Edge: GPS pause (gap in monotonic timestamps) — accepted by refine, projects as 30 s stall | `edition.schema.utils.test.ts:29-31` `it('accepts a non-uniform series modelling a pause', () => { expect(isMonotonicZeroToOne([0, 0.1, 0.9, 1])).toBe(true); })` (plan Risk #6). The "stall" behaviour itself is implicit in `projectFractionTimeAware` walking until `fractionAtIndex >= clamped`. | PASS |
| D10 | Edge: closed loop (first/last point at same lat/lng) | "Déjà géré par `projectFraction` actuel ; aucune logique nouvelle nécessaire" (spec line 49). `course-map.utils.test.ts:119-124` `it('handles a track of duplicated points (total === 0) by returning the first', …)` covers the linear path; `course-map.utils.test.ts:212-220` covers the time-aware zero-span path. | PASS |
| D11 | Edge: dense 2644-trkpt GPX → DTO size acceptable (~21 KB) | Spec line 50 marks this *out of scope v1* with "à monitorer" — no test obligation. | n/a (spec out-of-scope) |
| D12 | Error: corrupted `pointTimeFractions` in DB (non-monotonic / wrong bounds / length mismatch) → Zod refine rejects | `edition.schema.utils.test.ts:13-43` covers monotonicity (`[0.1, 1]` rejected, `[0, 0.9]` rejected, `[0, 0.5, 0.3, 1]` rejected, NaN rejected, duplicate `[0, 0.5, 0.5, 1]` rejected). Length-parity refine at `edition.schema.ts:19-24` — back-e2e covers the round-trip but the negative path (length mismatch) is not unit-tested in the core suite. | PASS (length-parity refine: PASS by code inspection; no dedicated negative unit test) |
| D13 | Error: `pointTimeFractions.length !== points.length` invariant at parse | Plan says "validé au parsing GPX (les deux tableaux sont produits ensemble)" + Zod refine on read. The parsing invariant holds by construction (`points` and `timestampsMs` are both `.map`-derived from `rawPoints`). | PASS (by-construction) |
| D14 | Spec line 18 input metric: DTO carries `pointTimeFractions` for timed GPX | Covered by D01 + `client.ts:33` (front-side Zod schema accepts the key) — no client-side test, the schema-parse is exercised by `apiClient.getCurrentEdition` runtime in browser. | PASS |
| D15 | Spec line 19 input metric: avatar at `timeFraction = 0.5` ≠ mid-distance position | Routed to `/visual-validation`. Surrogate at unit level: D03. | OUT-OF-SCOPE (visual) |
| D16 | Spec line 20 input metric: when `pointTimeFractions` absent, avatar follows mid-time-equals-mid-distance | Branch covered by `CourseMap.tsx:122-123`; the linear `projectFraction` itself has dedicated tests at `course-map.utils.test.ts:78-124`. No combined test asserting "branch X picks projector Y" at the component level (this is the visual-validation concern). | PASS (algorithm-level) |

## Notes

- A12 (`gpx_parse_partial_timings` breadcrumb at parse) — the spec line 149 calls for a server-side log when partial timings are detected at parse. The implementer shipped neither a Sentry breadcrumb (no `@sentry/node` in the Lambda) nor the planned `console.warn` downgrade. Plan §E flagged this as a follow-up; the diff is silent on the partial-timing observation. Strictly the input metric is unobservable in prod. Marked UNVERIFIABLE — the implementation deferred without writing the planned `console.warn` fallback.
- C05 (back-e2e) — not run by this validator per TL brief (parallel-Postgres contention). The implementor reported it green. The round-trip rows `'persists pointTimeFractions when present'` and `'absent pointTimeFractions stays undefined after a write→read cycle (no null leak)'` (`edition.repository.test.ts:46-87`) and the schema-utils 10-vector suite (`edition.schema.utils.test.ts`) all live in the back-e2e suite — coverage of A04 / A14 / D12 depends on this implementor-attested green.
- B11 (useEffect dep array, plan §F deviation) — the implementer added `pointTimeFractions` to the dep array of the runner-positioning effect, contrary to plan §F. The accompanying comment is correct: because `now` is already in the dep list and changes every poll tick, the additional `pointTimeFractions` reference cannot increase the effect's run frequency. The deviation is benign, justified for lint compliance, and documented. No over-rendering. Verdict PASS, with the deviation flagged for traceability per the brief.
- D12 length-parity negative path — the `length !== points.length` refine at `edition.schema.ts:19-24` is correct by code inspection but lacks a dedicated negative unit test (e.g. `points.length === 3, pointTimeFractions.length === 2` rejected). The refine is exercised by the round-trip test but only on length-matching inputs. Minor gap, not a FAIL.
- `course-map.utils.test.ts` does **not** inline the same `STRAVA_RECORDED_SAMPLE` as `gpx.core.test.ts` (plan §B / §C row's "matching pattern" suggested both files would). The site-side tests use synthesized `SAMPLE_POINTS` + custom `pointTimeFractions` arrays instead — a cleaner test pattern (no parsing dependency). This is a defensible simplification, not a defect.

## Verdict: PASS_EXCEPT_UNVERIFIABLE

The implementation faithfully reflects the spec across all 6 Q.O.D.s, all 11 "Files to change" entries (with plan-resolved deviation on `edition.repository.ts` documented), every plan-time inconsistency (§A null/undefined, §B/§C fixture inlining, §F dep-array), and the Zod refine bounds + length-parity contracts. Lint, typecheck, build, and `test:core` (with the 100 %-per-file coverage gate on `*.core.ts` and `site/**/*.utils.ts`) all pass. Two rows are UNVERIFIABLE: A12 (`gpx_parse_partial_timings` observation not landed in either Sentry or `console.warn` form — strict spec gap, plan-flagged as follow-up) and C05 (back-e2e suite deferred to implementor-attested green per TL brief). No FAIL rows.
