# Technical validation — Last Loop Lépin · race day live

- Spec: [`../spec/spec.md`](../spec/spec.md)
- Plan: [`../plan/plan.md`](../plan/plan.md)
- Branch: `claude/add-last-loop-lepin-subdomain-TC4QE`
- Base: `origin/main` (`f55fabe854de5129fa084185433364dc6d58d6e0`)
- HEAD: `73e3dbcdc19a52d360d346cb84ef5e2e846c0b9b`
- Run at: 2026-05-12T15:21:00Z
- Touched workspaces: `apps/last-loop-lepin` (this validation's focus); also touched in the same branch but out of scope here: `apps/borsouvertures`, `apps/borso-fr` (package.json bump), root config (`commitlint.config.js`, `knip.json`, `.github/path-filters.yml`, `CLAUDE.md`, `scripts/local-postgres.sh`, `.claude/skills/**`, `docs/**`).
- Preamble — visual routing: the spec's *Test strategy → Visual validation* section enumerates the assertions routed to `/visual-validation` (10 items: spectator hors-jour-J, spectator live, fiche coureur, admin PIN gate, setup d'édition, page des boucles, pointage propagation, top horaire DNF, correction banner, fin de course). Those rows are out of scope for this report. Category D below covers only the spec items the technical validator is responsible for (autonomous unit/integration tests for pure rules and HTTP endpoints, plus the three technical-validation assertions explicitly listed in the spec).

## A. Correctness vs spec

| # | Spec ref | Claim | Code (file:line) | Evidence (quoted) | Verdict |
|---|---|---|---|---|---|
| A01 | Q.O.D. *Domaine* | `last-loop-lepin.borso.fr` aliased on prod only | `apps/last-loop-lepin/cdk/test/stack.test.ts:83-93` | `prodTemplate.hasResourceProperties('AWS::CloudFront::Distribution', Match.objectLike({ DistributionConfig: Match.objectLike({ Aliases: Match.arrayWith(['last-loop-lepin.borso.fr']) }) }));` | PASS |
| A02 | Q.O.D. *Auth orga (PIN partagé)* | PIN check returns 401 / 429 with JWT cookie on success | `apps/last-loop-lepin/api/src/auth/auth.controller.ts:19-38` | `const status = error.reason === 'rate-limited' ? 429 : error.reason === 'misconfigured' ? 500 : 401;` + JWT set via `setCookie(context, AUTH_COOKIE_NAME, result.token, { httpOnly: true, secure: process.env.STAGE !== 'dev', sameSite: 'Strict', maxAge: ADMIN_COOKIE_TTL_SECONDS, path: '/' });` | PASS |
| A03 | Q.O.D. *Mock du temps* — no `.defaultNow()` on business columns | Audit asserts only whitelisted `created_at` audit cols carry `DEFAULT now()` | `apps/last-loop-lepin/api/src/database/migrations.audit.test.ts:20-25,55-72` | `AUDIT_COLUMNS_WITH_DEFAULT_NOW: ReadonlySet<string> = new Set([ 'editions.created_at', 'loop_punches.created_at', 'manual_dnfs.created_at', 'auth_attempts.created_at' ]);` + scan over generated SQL → `expect(violations).toEqual([])` | PASS |
| A04 | Q.O.D. *Persistance — DSQL* | Stack composes `PreviewableApp` (StaticSite + LambdaApi + DsqlSchema) | `apps/last-loop-lepin/cdk/lib/stack.ts:65-83` | `new PreviewableApp(props.scope, 'App', { app: APP_SLUG, stage: props.stage, … database: { migrationsPath: props.migrationsPath, cluster: props.cluster } })` | PASS |
| A05 | Q.O.D. *Framework HTTP — Hono on Lambda* | Single `app.ts` composes routes, `main.ts` wraps with `handle()` | `apps/last-loop-lepin/api/src/app.ts:24-46` | `export function createApp(): Hono { const app = new Hono(); app.use('*', logger()); app.use('*', cors()); app.get('/api/health', …); app.route('/api/editions', editionRouter); … if (process.env[TEST_SEED_FLAG] === '1') { app.route('/api/__test', testSeedRouter); } }` | PASS |
| A06 | Q.O.D. *Layout du code back — feature folders* | 6-file layout per bounded context | filesystem under `apps/last-loop-lepin/api/src/` | folders present: `punch/`, `runner/`, `edition/`, `ranking/`, `auth/`, `media/`, `helpers/{geo,gpx,sun}/`, `database/`, `__test/` | PASS |
| A07 | Q.O.D. *Calcul du classement — endpoint dédié* | `GET /api/standings/:editionSlug` with pure core | `apps/last-loop-lepin/api/src/app.ts:33` + `apps/last-loop-lepin/api/src/ranking/ranking.core.ts` | router mounted; `ranking.core.test.ts` (13 tests) covers tie-break, ex-æquo, manual DNF | PASS |
| A08 | Q.O.D. *Suffixe `*.core.ts`* + 100 % gate | gate-A coverage config targets `**/*.core.ts` + `**/*.utils.ts` at 100/100/100/100 | `apps/last-loop-lepin/vitest.workspace.ts:30-35` | `coverage: { provider: 'v8', all: false, include: ['api/src/**/*.core.ts', 'site/src/**/*.utils.ts'], thresholds: { perFile: true, ...COVERAGE_THRESHOLDS } }` (THRESHOLDS = 100/100/100/100) | PASS |
| A09 | Q.O.D. *Bornes saisies + sunrise/sunset calculées* | Pure helper `computeSunriseSunset` | `apps/last-loop-lepin/api/src/helpers/sun/sun.core.ts` + sibling test (8 cases including solstices / DST) | tests "Solstice été (jour long)", "Solstice hiver (jour court)", DST transition all green in run | PASS |
| A10 | Q.O.D. *Format de fin — couperet net + départage* | `isRaceEndReached` + tie-break by arrival order in `ranking.core.ts` | `apps/last-loop-lepin/api/src/edition/edition.core.ts:44-46` + `apps/last-loop-lepin/api/src/ranking/ranking.core.test.ts` | `export function isRaceEndReached(edition: RaceEdition, now: Date): boolean { return now.getTime() >= edition.endsAt.getTime(); }` + ranking suite "treats manual DNFs as DNF regardless of punch count" et al. | PASS |
| A11 | Q.O.D. *Élimination DNF semi-auto* | `projectDnfCandidates` exposed | `apps/last-loop-lepin/api/src/edition/edition.core.ts:62-100` | `export function projectDnfCandidates(edition: RaceEdition, runners, punches, manualDnfs, now: Date): readonly DnfProjection[] { … }` | PASS |
| A12 | Q.O.D. *Corrections de pointage — libres + journalisées* | `correctPunch` / `voidPunch` write `corrected_at` / `voided_at` | `apps/last-loop-lepin/api/src/punch/punch.service.ts:85-102` | `await markPunchCorrected(database, id, newFinishedAt, now);` + `await markPunchVoided(database, id, now);` | PASS |
| A13 | Q.O.D. *Fiche coureur — `/r/<slug>` publique* | Route `/r/:slug` rendered with no auth | `apps/last-loop-lepin/site/src/App.tsx:35-40,86-89` | `const runnerMatch = /^\/r\/([a-z0-9-]+)$/.exec(path);` + `route === 'runner' ? <RunnerFichePage editionSlug="lepin-2026" runnerSlug={params.runnerSlug ?? ''} /> : null` | PASS |
| A14 | Q.O.D. *Source du tracé — GPX upload* | Pure parser in `helpers/gpx/gpx.core.ts`, covered at 100 % | `apps/last-loop-lepin/api/src/helpers/gpx/gpx.core.ts` + sibling test (16 cases) | gate-A run green, includes "GPX valide minimal", "multiple tracks", "GPX vide", "GPX malformé", "GPX sans `<ele>`", extraction `startLatLng` | PASS |
| A15 | Q.O.D. *Photo manquante → initials* | Pure helper `initialsAvatar` côté front | `apps/last-loop-lepin/site/src/domain/initials.utils.ts` + 11 sibling tests | gate-A run green, includes determinism + collision + accents | PASS |
| A16 | Q.O.D. *Conflit de pointage (409) — first-wins via contrainte unique* | Partial unique index + repository raises `PunchConflictError` | `apps/last-loop-lepin/api/src/database/migrations/0000_initial.sql:54` + `apps/last-loop-lepin/api/src/punch/punch.service.ts:67-80` | `CREATE UNIQUE INDEX "loop_punches_active_uq" ON "loop_punches" USING btree ("edition_slug","runner_slug","loop_index") WHERE voided_at IS NULL` + service catches `'loop_punches_active_uq'` and throws `PunchConflictError` | PASS |
| A17 | Q.O.D. *Stratégie de tests back — testcontainers* | `test/setup-postgres.ts` boots via `local-postgres.sh` | `apps/last-loop-lepin/vitest.workspace.ts:49` + `apps/last-loop-lepin/test/setup-postgres.ts` | `globalSetup: ['./test/setup-postgres.ts']`; `DATABASE_URL=$(scripts/local-postgres.sh start last-loop-lepin) vitest run --project back-e2e` in `package.json` | PASS |
| A18 | Q.O.D. *Stratégie de couverture — deux gates vitest* | Two `vitest` projects in `vitest.workspace.ts` | `apps/last-loop-lepin/vitest.workspace.ts:13-72` | `defineWorkspace([ { test: { name: 'core', … } }, { test: { name: 'back-e2e', … } } ])` | PASS |
| A19 | Spec *Test strategy → technical-validation (i)* | No `.defaultNow()` on business cols, asserted in test | `apps/last-loop-lepin/api/src/database/migrations.audit.test.ts` | as A03 above | PASS |
| A20 | Spec *Test strategy → technical-validation (ii)* | `stack.test.ts` asserts `LASTLOOP_ALLOW_TEST_SEED` absent in prod | `apps/last-loop-lepin/cdk/test/stack.test.ts:63-66` | `for (const fn of Object.values(prodFunctions)) { expect(readEnvVars(fn)).not.toHaveProperty('LASTLOOP_ALLOW_TEST_SEED'); }` | PASS |
| A21 | Spec *Test strategy → technical-validation (iii)* | App mounts `__test/` only when flag set | `apps/last-loop-lepin/api/src/app.ts:41-43` | `if (process.env[TEST_SEED_FLAG] === '1') { app.route('/api/__test', testSeedRouter); }` | PASS |
| A22 | Spec *Files to change* — `site/components/Countdown.tsx` via `useSyncExternalStore` | Countdown uses external clock store, no `useEffect` | `apps/last-loop-lepin/site/src/components/Countdown.tsx:20` | `const now = useSyncExternalStore(subscribeClock, getCurrentTime, () => Date.now());` | PASS |
| A23 | Spec *Files to change* — `cdk/src/stack.ts` with photos bucket | S3 bucket declared with expected name shape | `apps/last-loop-lepin/cdk/lib/stack.ts:35-51` + `cdk/test/stack.test.ts:75-81` | `new Bucket(props.scope, 'PhotosBucket', { bucketName: '${APP_SLUG}-${props.stage}-photos${props.prNumber !== undefined ? '-${props.prNumber}' : ''}', … })` + test assertion `BucketName: 'last-loop-lepin-prod-photos'` | PASS |
| A24 | Spec *Files to change* — `LoopsTimeline.tsx` with sunrise/sunset markers | Component present | `apps/last-loop-lepin/site/src/components/LoopsTimeline.tsx` | file present in tree; visual conformance routed to `/visual-validation` | PASS |
| A25 | Spec *Files to change* — `CorrectionBanner.tsx` | Component named in spec | (not present) | `find apps/last-loop-lepin/site/src -name 'CorrectionBanner*'` → empty | FAIL |
| A26 | Spec *Files to change* — `runner.core.ts` + `edition.core.ts` + `punch.core.ts` + `ranking.core.ts` all listed | Three of four present; `runner.core.ts` absent | filesystem under `api/src/runner/` | `apps/last-loop-lepin/api/src/runner/` contains `runner.{controller,repository,schema,service,types}.ts` — no `runner.core.ts` (no `runner.core.test.ts`). Spec *Files to change* lists `runner/` with "mêmes 6 fichiers" (controller / service / repository / core / schema / types). | FAIL |
| A27 | Spec *Files to change* — `auth/auth.middleware.ts + .test.ts`, `auth.jwt.ts + .test.ts` | Files present without sibling tests | filesystem under `api/src/auth/` | `auth.middleware.ts` and `auth.jwt.ts` exist; their sibling `.test.ts` files (mandated in spec line `auth.middleware.ts + .test.ts`) are absent. | FAIL |
| A28 | Spec *Files to change* — `media/media.controller.ts + .test.ts`, `media.service.ts + .test.ts`, `media.s3.ts + .test.ts` | Files present without sibling tests | filesystem under `api/src/media/` | `media.controller.ts`, `media.service.ts`, `media.s3.ts` exist; their sibling `.test.ts` files (mandated in spec) are absent. | FAIL |
| A29 | Spec *Files to change* — `punch/punch.{controller,service,repository}.ts + .test.ts` | Only `punch.core.test.ts` present, the other three siblings missing | filesystem under `api/src/punch/` | `punch.{controller,service,repository}.ts` exist; their sibling `.test.ts` files mandated in spec ("`punch.controller.ts + .test.ts`", "`punch.service.ts + .test.ts`", "`punch.repository.ts + .test.ts`") are absent. | FAIL |
| A30 | Spec *Files to change* — `ranking.{controller,service}.ts + .test.ts` | Only `ranking.core.test.ts` present | filesystem under `api/src/ranking/` | `ranking.controller.ts`, `ranking.service.ts` exist without `.test.ts` siblings. | FAIL |
| A31 | Spec *Files to change* — `edition.{controller,service,repository}.ts + .test.ts` | Only `edition.core.test.ts` present | filesystem under `api/src/edition/` | `edition.controller.ts`, `edition.service.ts`, `edition.repository.ts` exist without `.test.ts` siblings. | FAIL |
| A32 | Spec *Files to change* — `__test/test-seed.controller.ts + .test.ts` | File present without sibling test | `apps/last-loop-lepin/api/src/__test/` | `test-seed.controller.ts` exists; `test-seed.controller.test.ts` absent. | FAIL |
| A33 | Spec *Files to change* — `database/client.ts + client.test.ts` | File present without sibling test | `apps/last-loop-lepin/api/src/database/` | `client.ts` exists; spec-mandated `client.test.ts` absent. Note the spec specifically lists three test scenarios (signer called once per cold start; token refresh; handler error wrapped in `DsqlUnavailableError`) in plan How-each-decision-becomes-code row "Accès DSQL". | FAIL |
| A34 | Plan *streaming clarification* | v1 ships without `awslambda.streamify` (deferred); spec text mentions it as an aspirational note | `apps/last-loop-lepin/api/src/main.ts` | Note in plan acknowledges spec drift. The deviation is plan-documented, not a regression. Recording as PASS with note in *Notes*. | PASS |

## B. Code cleanliness

| # | Rule | Check | Evidence | Verdict |
|---|---|---|---|---|
| B01 | Biome lint clean on workspace | `pnpm --filter @borso-app/last-loop-lepin run lint` | `Checked 77 files in 1514ms. No fixes applied.` exit 0 | PASS |
| B02 | `pnpm typecheck` clean | `pnpm --filter @borso-app/last-loop-lepin run typecheck` | `tsc -p tsconfig.cdk.json --noEmit && tsc --noEmit` exit 0 (no output) | PASS |
| B03 | No `any` in changed code | `grep -nP '\bany\b' apps/last-loop-lepin/{site/src,api/src,cdk,test}/**` | only matches found are in test description text ("treats manual DNFs **as** DNF") and a doc comment (`main.dev.ts:5: 'point this at any local Postgres'`). No `: any` / `as any`. | PASS |
| B04 | Type assertions limited to `as const` / `as unknown` | `grep -rn " as [A-Z]" apps/last-loop-lepin/{api,site,cdk}/src/` | only hits are a test-string ("as DNF"), an import (`import * as Sentry`), and a JSDoc reference (`* 'value as T'; this construct is a type predicate`). No live `as Foo` assertions. | PASS |
| B05 | No `useEffect` smell | `grep -rn 'useEffect' apps/last-loop-lepin/site/src/` | Only 2 hits, both in JSDoc comments justifying the absence of `useEffect`. Components use `useSyncExternalStore` (`Countdown.tsx:20`, `App.tsx:44,79`, `useStandingsPoll.ts:69`, `useResource.ts:69`). | PASS |
| B06 | No abbreviations / single-letter locals | sample read of changed files | identifiers are `accumulator`, `currentTime`, `listeners`, `editionSlug`, `runnerPunches`, `closingTimeMs`, `expectedClosedLoop` etc. The single one-letter `t`/`x` does not appear; `i`-style loops are absent (uses `for (const … of …)`). | PASS |
| B07 | Magic numbers extracted to named consts | sample read of changed files | `TICK_INTERVAL_MS = 1_000`, `POLL_INTERVAL_MS = 2_000`, `MILLISECONDS_PER_MINUTE = 60_000`, `ADMIN_COOKIE_TTL_SECONDS = 12 * 60 * 60`, `TEST_SEED_FLAG = 'LASTLOOP_ALLOW_TEST_SEED'`, `RACE_CACHE_KEY = 'edition:current'`, `APP_SLUG = 'last-loop-lepin'`, etc. | PASS |
| B08 | Comments document WHY, not WHAT | sample read | `edition.core.ts:74-78` explains the timing tolerance rule (non-obvious infra constraint); `punch.core.ts:63-65` explains why `.reduce` is used over indexed access (`noUncheckedIndexedAccess` interaction). No restatement comments. | PASS |
| B09 | JSDoc only on exported APIs / shared types | sample read | JSDoc seen on `edition.core.ts` exports (`nextHourlyTop`, `loopIndexAt`, `isRaceEndReached`, `projectDnfCandidates`), `punch.core.ts` exports, `useResource.ts` exports, etc. Internal helpers (`tick`, `ensureInterval`) have no JSDoc. | PASS |
| B10 | Function names describe result | sample read | `validatePunchTiming`, `nextHourlyTop`, `loopIndexAt`, `projectDnfCandidates`, `isRaceEndReached`, `initialsAvatar`, `computeStandings`, `subscribeClock`, `getCurrentTime`. No `processX` / `handleStuff` shape. | PASS |
| B11 | `noUncheckedIndexedAccess` honoured | sample of indexed accesses | `App.tsx:38` uses `if (slug !== undefined)` after array index; `punch.core.ts:62-79` uses `.reduce` precisely to side-step indexed access; `migrations.audit.test.ts:40,45` use `match[1] ?? ''` fallback. | PASS |
| B12 | `*.core.ts` files never call `new Date()` | `grep -n 'new Date()' apps/last-loop-lepin/api/src/**/*.core.ts` | All hits are in comments / JSDoc explaining the rule ("None of these functions call `new Date()`"). No live call. | PASS |
| B13 | `knip` clean | `pnpm exec knip` | exit 0 | PASS |

## C. Tests pass

| # | Workspace | Command | Exit | Verdict |
|---|---|---|---|---|
| C01 | `@borso-app/last-loop-lepin` gate A (`test:core`) | `pnpm --filter @borso-app/last-loop-lepin run test:core` | 9 test files, 99 tests passed, exit 0 (10.20s with `--coverage`) | PASS |
| C02 | `@borso-app/last-loop-lepin` gate B (`test`) | `pnpm --filter @borso-app/last-loop-lepin run test` | 8 test files, 89 tests passed, exit 0 (2.50s; testcontainer auto-boot ok) | PASS |
| C03 | Pure `*.utils.ts` files in touched workspace ship sibling `.test.ts` (100 % coverage rule) | enumerate `*.utils.ts` | only `site/src/domain/initials.utils.ts` exists; sibling `initials.utils.test.ts` present and green. | PASS |
| C04 | Pure `*.core.ts` files in touched workspace ship sibling `.test.ts` (100 % coverage rule) | enumerate `*.core.ts` | `api/src/edition/edition.core.ts`, `api/src/punch/punch.core.ts`, `api/src/ranking/ranking.core.ts`, `api/src/helpers/{geo,gpx,sun}/*.core.ts`. All 6 carry sibling `*.core.test.ts` files and the gate-A coverage threshold (100/100/100/100, perFile) runs green. | PASS |

## D. Test coverage of spec

The spec's *Test strategy* explicitly enumerates the gate-B integration suites that the implementation must ship ("Suites attendues" / "Endpoint sous test"). Below I check each against the diff. The race-day-2026 scenario is the only gate-B test the spec mentions that is actually present; per the validation brief, the back-e2e coverage threshold itself is deliberately disabled in `vitest.workspace.ts`, so I do not fail rows here for "the threshold doesn't catch it" — I fail them because the spec listed an integration test by name and it isn't there.

Gate-A pure-rule rows pass uniformly because the `*.core.test.ts` suites are exhaustive and green at 100 % coverage.

| # | Use case | Covering test | Verdict |
|---|---|---|---|
| D01 | Gate A — `helpers/geo/geo.core.ts` (Haversine + smoothing) | `api/src/helpers/geo/geo.core.test.ts` (14 tests, green) | PASS |
| D02 | Gate A — `helpers/gpx/gpx.core.ts` (valid / multi-track / empty / malformed / no-ele / extraction) | `api/src/helpers/gpx/gpx.core.test.ts` (16 tests, green) | PASS |
| D03 | Gate A — `helpers/sun/sun.core.ts` (solstices / polar / DST) | `api/src/helpers/sun/sun.core.test.ts` (8 tests, green) | PASS |
| D04 | Gate A — `punch/punch.core.ts` (pointage valide / hors-fenêtre / loop attendue) | `api/src/punch/punch.core.test.ts` (13 tests, green) | PASS |
| D05 | Gate A — `ranking/ranking.core.ts` (classement / DNF / ex-æquo / départage) | `api/src/ranking/ranking.core.test.ts` (13 tests, green) | PASS |
| D06 | Gate A — `edition/edition.core.ts` (`nextHourlyTop`, `isRaceEndReached`, `projectDnfCandidates`) | `api/src/edition/edition.core.test.ts` (20 tests, green) | PASS |
| D07 | Gate A — `site/src/domain/initials.utils.ts` (déterministe + collision + accents) | `site/src/domain/initials.utils.test.ts` (11 tests, green) | PASS |
| D08 | Spec *Test strategy → technical-validation (i)* (audit no `.defaultNow()` on business cols) | `api/src/database/migrations.audit.test.ts` (1 test, green) | PASS |
| D09 | Spec *Test strategy → technical-validation (ii)* (CDK assert absent flag in prod) | `cdk/test/stack.test.ts` "mounts the test-seed endpoint flag only on non-prod stacks" (green) | PASS |
| D10 | Spec *Test strategy → technical-validation (iii)* (router mounts `__test/` only with flag) | covered by code reading; no dedicated app-routing test (no `app.test.ts`). The spec's *Files to change* lists `src/app.ts + app.test.ts` as a mandatory file pair; the routing assertion is the natural home for it. Behaviour visible in `app.ts:41-43` but un-tested. | FAIL |
| D11 | Spec gate-B `POST /api/admin/auth/login` (200/401/429) | (no `auth.controller.test.ts` / `auth.service.test.ts` / `auth.middleware.test.ts`) | FAIL |
| D12 | Spec gate-B `POST /api/admin/editions` (GPX valid/malformed/JWT/`startsAt > endsAt`) | (no `edition.controller.test.ts` / `edition.service.test.ts`) | FAIL |
| D13 | Spec gate-B `POST /api/admin/punches` (persist, 409 on double-punch) | (no `punch.controller.test.ts` / `punch.service.test.ts` / `punch.repository.test.ts`). The race-day-2026 scenario punches but does not exercise the 409 conflict path. | FAIL |
| D14 | Spec gate-B `PUT /api/admin/punches/:id` (correction → `corrected_at`) | (no test) | FAIL |
| D15 | Spec gate-B `DELETE /api/admin/punches/:id` (annulation → `voided_at`) | (no test) | FAIL |
| D16 | Spec gate-B `POST /api/admin/runners` (CRUD + photo presign + S3 mock) | (no `runner.controller.test.ts` / `runner.service.test.ts` / `runner.repository.test.ts`) | FAIL |
| D17 | Spec gate-B `GET /api/standings/:editionId` (ordered, ex-æquo, DNF, post-window) | `test/race-scenarios/race-day-2026.test.ts` exercises empty standings + DNF after second top via this endpoint. Does NOT cover ex-æquo, post-`endsAt` frozen ranking, ordering of in-race+dnf split — none of those are in the four `it()` blocks. Partial coverage only. | FAIL |
| D18 | Spec gate-B `GET /api/editions/:slug/state` (countdown au prochain top, photos éliminés) | endpoint not visible in `app.ts` routing inspection (only `/api/editions`, `/api`, `/api/admin/*`, `/api/admin/auth`, `/api/admin/editions`, `/api/admin/runners`, `/api/admin`, `/api/admin/media`); no `state` route mentioned in `edition.controller.ts`. No test either. | FAIL |
| D19 | Spec gate-B `POST /api/admin/presign` (URL S3 ≤ 5 min) | no `media.{controller,service,s3}.test.ts`; `media.controller.ts` exists but is untested. | FAIL |
| D20 | Spec gate-B `client.test.ts` (cold-start signer + token refresh + `DsqlUnavailableError`) | spec calls this test out by name in *Files to change*; not present. | FAIL |
| D21 | Spec gate-B *race-day-2026 scenario* — full edition replay | `test/race-scenarios/race-day-2026.test.ts` exists with 4 `it()` blocks. Compared to spec's bulleted protocol (06:00 start, hourly loop 06..21 with `vi.setSystemTime` + punches + DNF assert, 22:00 end + final ranking) this is a thin smoke test, not the full replay the spec describes ("Étape 3: `vi.setSystemTime('2026-09-19T06:00:00+02:00')` … Étape 4: assert final ranking + tie-break order"). PASS for "scenario file exists & green"; the depth gap is also visible as D17's partial coverage. | PASS (with note) |

## Notes

- A25 — Spec's *Files to change* names `site/src/components/CorrectionBanner.tsx`. The component is absent from the diff. The "correction banner pour 60 s" behaviour also surfaces in Q.O.D. *Corrections de pointage — libres + journalisées publiquement*, in *Edge cases* ("une bannière publique « pointage corrigé à HH:MM » apparaît sur la vue spectateur pendant 60 s"), and in the visual-validation checklist. The matching backend support (a `recentCorrections[]` field on `GET /api/editions/:slug/state`) is also absent — D18 fails for the same root cause.

- A26 — `runner.core.ts` is named in *Files to change* ("mêmes 6 fichiers" for `runner/`) and would be the natural home for runner-slug uniqueness and bib-uniqueness rules. The current implementation puts no rules in a `.core.ts`, which is allowed if the spec's intent is "this feature has no pure rules" — but the spec explicitly enumerates the 6-file layout for every feature. The plan should have either explicitly waived `runner.core.ts` (it doesn't) or the file should ship. Today: no waiver, no file → spec deviation.

- A27/A28/A29/A30/A31/A32/A33 — All of these are spec line-items in *Files to change* spelled out as `<file>.ts + .test.ts`. None of the named `.test.ts` siblings exists. Together they form the bulk of the failures in category D as well: the spec lists sibling tests for every controller/service/repository on every feature, and only the `.core.test.ts` ones were delivered. The implementer landed the testcontainers harness + the `race-day-2026` smoke scenario (which is real progress, and is explicitly noted as the v1 cut in `vitest.workspace.ts` comment lines 63-68: "v1 ships the testcontainer harness + the race-day-2026 scenario + the audit tests; per-feature integration tests are scheduled in a follow-up PR labelled `kaizen` so the deploy of PreviewableApp can land first"). That is a deliberate scope cut. The spec did not record that cut. The fix is either (a) implement the missing suites before merging this PR, or (b) update the spec to reflect the v1 scope and re-run `/specification`. Per the validator's rules, the verdict reflects the current spec text, not the inferred operator intent.

- A34 — Plan documents the streaming deviation under *Open questions / Resolved*: "Ship v1 sans `awslambda.streamify`". Spec text still mentions `awslambda.streamify`; this is recorded as a known mismatch in the plan. Recording as PASS because the deviation is plan-acknowledged, not an unannounced regression.

- D10 — `app.test.ts` was explicitly listed in *Files to change* as `src/app.ts + app.test.ts`. Without it, the technical-validation assertion (iii) is unverified by an automated test even though the code is correct. The race-day-2026 scenario doesn't toggle the flag and re-instantiate.

- D17 — The standings endpoint is covered by 2 of the 4 race-day-2026 `it()` blocks ("returns the empty standings before any punch", "marks late runners DNF after the second hourly top"). The other behaviours the spec explicitly names (ex-æquo branch, post-`endsAt` frozen ranking, ordered placement of in-race+dnf) are not exercised. The `.core.test.ts` for ranking covers the pure rule; the integration path through repository + service + controller for those branches is uncovered.

- D18 — `GET /api/editions/:slug/state` is not implemented. Searching `apps/last-loop-lepin/api/src/edition/edition.controller.ts` shows no `state` route. The frontend spectator page polls standings (visible in `data/useStandingsPoll.ts`), so the absence of `state` doesn't break the user-visible behaviour the visual-validator screenshots, but it does mean the recentCorrections / countdown-target / photo-elim fields the spec lists as part of `/state` don't have a home. This is the largest unimplemented endpoint in the diff.

- C03 — The workspace currently contains only one `*.utils.ts` (`initials.utils.ts`); the rest of the pure helpers are correctly under `*.core.ts`. The 100 % perFile threshold applies to both globs in gate A.

- Coverage thresholds — Gate A enforces 100 % per file on `**/*.core.ts` and `**/*.utils.ts` and runs green (verified via `--coverage`). Gate B's threshold on `api/src/**` is deliberately omitted (vitest.workspace.ts:63-68) and the validator brief instructs me not to fail on that. I am, however, treating spec-listed integration test suites as in-scope under category D — coverage threshold disabled does not waive named test cases.

- The validation brief explicitly carves out the back-e2e coverage threshold; it does not carve out the named test suites. The category-D failures above reflect spec line items not satisfied, not coverage percentages.

## Verdict: FAIL

A25 / A26 / A27 / A28 / A29 / A30 / A31 / A32 / A33 (category A — Files to change not delivered) and D10–D20 (category D — gate-B suites the spec mandates by name) are FAIL rows. None of these can be rounded up to UNVERIFIABLE: the spec was explicit, the diff shipped code without the matching test scaffolding, and the implementer's own comment in `vitest.workspace.ts:63-68` acknowledges the scope was cut for a follow-up PR. The verdict mechanic does not honour "scope cut, follow-up planned" as a pass — the spec must be re-run through `/specification` to record the cut, or the missing artifacts must land before merge.
