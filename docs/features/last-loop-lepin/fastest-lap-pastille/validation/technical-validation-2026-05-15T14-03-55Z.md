# Technical validation — Une pastille violette signale le coureur du tour le plus rapide

- Spec: [`../spec/spec.md`](../spec/spec.md)
- Plan: [`../plan/plan.md`](../plan/plan.md)
- Branch: worktree-agent-a593a676f539d8550 (sub-agent worktree branched from `claude/fix-dnf-validation-NIGKH` at `febcfdc`)
- Base: `9268a4d` (commit immediately before this feature's plan was added; diff filtered to the feature file surface in plan.md *Files to change*)
- Run at: 2026-05-15T14:03:55Z
- Touched workspaces: `@borso-app/last-loop-lepin` (api + site)

> Routing note: the spec's *Test strategy* assigns the four user-visible badge assertions (badge on the seeded fastest runner; double-badge on a ms tie; no badge before the first close; badge retained on a DNF holder) to `/visual-validation`. Category D below covers the remaining pure-back / DTO assertions only — UI runtime checks are out of scope for this report.

## A. Correctness vs spec

| # | Spec ref | Claim | Code (file:line) | Evidence (quoted) | Verdict |
|---|---|---|---|---|---|
| A01 | Q.O.D. "Définition meilleur tour" = running time (option b) | `fastestLap` reuses `loopDurationMs(edition, punch)` — same formula as `lastLoopDurationMs` | apps/last-loop-lepin/api/src/punch/punch.core.ts:71-76 | `export function loopDurationMs(edition: RaceEdition, punch: LoopPunch): number \| null { … const loopStartMs = edition.startsAt.getTime() + (punch.loopIndex - 1) * intervalMs; const elapsed = punch.finishedAt.getTime() - loopStartMs; return elapsed >= 0 ? elapsed : null; }` | PASS |
| A02 | Q.O.D. "Tie-break à la ms" = both decorated (option a) | Reducer collects every entry equal to the running min; result.length ≥ 2 carries both runners | apps/last-loop-lepin/api/src/ranking/fastest-lap.core.ts:55-68 | `const minimum = candidates.reduce(...); … for (const candidate of candidates) { if (candidate.durationMs !== minimum) continue; if (seenSlugs.has(candidate.runnerSlug)) continue; seenSlugs.add(...); result.push(...); }` | PASS |
| A03 | Q.O.D. "DNF holder reste" (option a) | `fastest-lap.core.ts` reads only `LoopPunch[]`, never consults `ManualDnf` or `RunnerStatus` | apps/last-loop-lepin/api/src/ranking/fastest-lap.core.ts:16-18 | `import type { RaceEdition } from '../edition/edition.types'; import { loopDurationMs } from '../punch/punch.core'; import type { LoopPunch } from '../punch/punch.types';` — no DNF import | PASS |
| A04 | Q.O.D. "Visuel" = round violet, ~16px, top-right (option a) | `.leaderboard-chip__fastest-lap-badge` positioned absolute top/right with violet bg + 16px size | apps/last-loop-lepin/site/src/styles/leaderboard.css:39-54 | `position: absolute; top: var(--badge-offset); right: var(--badge-offset); width: var(--badge-size); height: var(--badge-size); border-radius: 50%; background: var(--accent-fastest-lap, #a020f0);` with `--badge-size: 16px; --badge-offset: -6px;` declared inline | PASS |
| A05 | Q.O.D. "Surface UI = chip only" (option a) | `CourseMap.tsx` not touched for this feature; only chip carries the badge | apps/last-loop-lepin/site/src/components/Leaderboard.tsx:85 | `{hasFastestLap ? <FastestLapBadge /> : null}` — single render site, inside chip only | PASS |
| A06 | Q.O.D. "Forme DTO" = `ReadonlyArray<{ runnerSlug, durationMs }>` (option b) | Back DTO and front DTO match; non-optional on the back, optional-with-runtime-normalisation on the front | apps/last-loop-lepin/api/src/ranking/ranking.types.ts:25-28 | `readonly fastestLap: ReadonlyArray<{ readonly runnerSlug: string; readonly durationMs: number }>;` | PASS |
| A07 | Q.O.D. "Pas de `loopIndex` dans le DTO v1" (option b) | Zod schema for entries declares only `runnerSlug` + `durationMs`; same for `FastestLapEntry` type | apps/last-loop-lepin/site/src/api/client.ts:75-77 | `fastestLap: z.array(z.object({ runnerSlug: z.string(), durationMs: z.number() })).optional(),` — no `loopIndex` | PASS |
| A08 | Files-to-change → `fastest-lap.core.ts` (new) | New pure module with single exported `fastestLap` function | apps/last-loop-lepin/api/src/ranking/fastest-lap.core.ts:42-69 | `export function fastestLap(edition: RaceEdition, punches: readonly LoopPunch[]): ReadonlyArray<FastestLapEntry> { … }` | PASS |
| A09 | Files-to-change → `fastest-lap.core.test.ts` (new, 6+ vectors) | 9 `it(...)` cases incl. empty / all-voided / one runner / multiple runners / ms tie / same-runner dedup / DNF retention / clock skew / order-independence | apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:46-136 | `describe('fastestLap', () => { it('returns [] when no punches are recorded'…); it('returns [] when every punch is voided'…); … it('keeps the record on a DNF holder'…); it('returns [] when every punch yields a null duration (clock-skew degenerate)'…); it('is order-independent'…); });` | PASS |
| A10 | Files-to-change → `punch.core.ts` refactor (extract `loopDurationMs`) | Helper exported; `lastLoopDurationMs` rewritten on top; clock-skew `null` branch preserved verbatim | apps/last-loop-lepin/api/src/punch/punch.core.ts:71-98 | `export function loopDurationMs(edition: RaceEdition, punch: LoopPunch): number \| null { … return elapsed >= 0 ? elapsed : null; } … export function lastLoopDurationMs(…) { const lastPunch = … .reduce<LoopPunch \| null>((_, punch) => punch, null); if (lastPunch === null) return null; return loopDurationMs(edition, lastPunch); }` | PASS |
| A11 | Files-to-change → `punch.core.test.ts` extended | New `describe('loopDurationMs')` block with 3 direct vectors (positive, clock-skew null, boundary zero) | apps/last-loop-lepin/api/src/punch/punch.core.test.ts:141-156 | `describe('loopDurationMs', () => { it('returns the elapsed time from the loop boundary to finishedAt'…); it('returns null when finishedAt precedes the loop boundary (clock skew)'…); it('returns 0 when finishedAt equals the loop boundary exactly'…); });` | PASS |
| A12 | Files-to-change → `ranking.types.ts` updated | `Standings` gains non-optional `fastestLap` field of the spec'd shape | apps/last-loop-lepin/api/src/ranking/ranking.types.ts:25-28 | `readonly fastestLap: ReadonlyArray<{ readonly runnerSlug: string; readonly durationMs: number }>;` | PASS |
| A13 | Files-to-change → `ranking.core.ts` calls `fastestLap()` | Import + single call after `validPunches` computation; forwards into returned `Standings` | apps/last-loop-lepin/api/src/ranking/ranking.core.ts:21,170 | `import { fastestLap } from './fastest-lap.core';` and in the return: `fastestLap: fastestLap(edition, validPunches),` | PASS |
| A14 | Files-to-change → `ranking.core.test.ts` extended | Two new `it(...)` blocks dedicated to `fastestLap` (empty before any punch; populated after) | apps/last-loop-lepin/api/src/ranking/ranking.core.test.ts:208-228 | `it('returns an empty fastestLap before any punch is recorded'…); it('surfaces the fastest runner in fastestLap after a few loops, with the expected durationMs'…)` | PASS |
| A15 | Files-to-change → `site/src/domain/types.ts` updated | `StandingsDto` mirror gains the same shape as back `Standings` | apps/last-loop-lepin/site/src/domain/types.ts:88-91 | `readonly fastestLap: ReadonlyArray<{ readonly runnerSlug: string; readonly durationMs: number }>;` | PASS |
| A16 | Files-to-change → `site/src/api/client.ts` Zod schema | `standingsSchema` declares `fastestLap` optional (deploy-gap tolerance) | apps/last-loop-lepin/site/src/api/client.ts:75-77 | `fastestLap: z.array(z.object({ runnerSlug: z.string(), durationMs: z.number() })).optional(),` | PASS |
| A17 | Files-to-change → `Leaderboard.tsx` reads & renders | Accepts `fastestLapSlugs: ReadonlySet<string>`; renders `<FastestLapBadge />` per chip when slug is in set | apps/last-loop-lepin/site/src/components/Leaderboard.tsx:12,82,85 | `readonly fastestLapSlugs?: ReadonlySet<string>;` + `const hasFastestLap = decoratedSlugs.has(entry.runner.slug);` + `{hasFastestLap ? <FastestLapBadge /> : null}` | PASS |
| A18 | Files-to-change → `leaderboard.css` + `tokens.css` new rule + token | Rule + violet token declared | apps/last-loop-lepin/site/src/styles/tokens.css:31 + leaderboard.css:39-54 | `--accent-fastest-lap: #a020f0;` (tokens) + `.leaderboard-chip__fastest-lap-badge { … background: var(--accent-fastest-lap, #a020f0); … }` (leaderboard) | PASS |
| A19 | Plan §A — refactor preserves clock-skew `null` branch | Branch lives in `loopDurationMs` (only place); `lastLoopDurationMs` propagates via `if (lastPunch === null) return null; return loopDurationMs(…)` | apps/last-loop-lepin/api/src/punch/punch.core.ts:75 | `return elapsed >= 0 ? elapsed : null;` — single occurrence (verified by grep, exactly one match repo-wide) | PASS |
| A20 | Plan §A — `lastLoopDurationMs` semantics stay equivalent | Signature unchanged `(edition, runnerSlug, validPunchesForRunner) => number \| null`; existing 5 tests + 2 new "loopIndex (not array length)" / "missing intermediate" still pass | apps/last-loop-lepin/api/src/punch/punch.core.test.ts:97-138 | `describe('lastLoopDurationMs', () => { … });` — all 6 pre-existing cases green (vitest exit 0) | PASS |
| A21 | Plan §A — `punch.core.ts` stays at 100 % coverage | v8 coverage report shows 100 % S/B/F/L for `api/src/punch/punch.core.ts` | (coverage run, this report's *Notes*) | `100.0% S \| 100.0% B \| 100.0% F \| api/src/punch/punch.core.ts` from `coverage-final.json` | PASS |
| A22 | Plan §I — `<Leaderboard>` callsites: plan listed 4, impl found 1 | Re-grep confirms one callsite (`SpectatorPage.tsx:182`). `AdminPage.tsx`, `RunnerFichePage.tsx`, `ArchivesPage.tsx` exist as routes but do NOT import `Leaderboard` | `grep -l "Leaderboard" apps/last-loop-lepin/site/src/routes/*.tsx` → `SpectatorPage.tsx` only | `apps/last-loop-lepin/site/src/routes/SpectatorPage.tsx:7:import { Leaderboard } from '../components/Leaderboard';` (single match) | PASS |
| A23 | Plan §I — every callsite passes `fastestLapSlugs` | The single SpectatorPage callsite builds the Set inline | apps/last-loop-lepin/site/src/routes/SpectatorPage.tsx:182-188 | `<Leaderboard ranked={standings?.ranked ?? []} fastestLapSlugs={ new Set((standings?.fastestLap ?? []).map((entry) => entry.runnerSlug)) } onChipSelect={setSelectedRunner} />` | PASS |
| A24 | Zod `.optional().default([])` deviation — impl moved normalisation to `useStandingsPoll.ts` | Schema uses `.optional()` only; the snapshot construction normalises `?? []` so consumers always see an array (`StandingsDto.fastestLap` stays non-optional) | apps/last-loop-lepin/site/src/data/useStandingsPoll.ts:62-72 | `// Normalise the deploy-gap optional: … `fastestLap` … Zod parses as `undefined`. Pin it to `[]` here … entry.snapshot = { standings: { ...response.standings, fastestLap: response.standings.fastestLap ?? [], }, … }` — server omits → client sees `[]` semantic holds end-to-end | PASS |

## B. Code cleanliness

| # | Rule | Check | Evidence | Verdict |
|---|---|---|---|---|
| B01 | Biome lint clean | `pnpm exec biome lint <14 feature files>` | `Checked 14 files in 3s. No fixes applied.` (exit 0) | PASS |
| B02 | Typecheck clean | `pnpm --filter @borso-app/last-loop-lepin run typecheck` | exit 0 — `tsc -p tsconfig.cdk.json --noEmit && tsc --noEmit` | PASS |
| B03 | No `any` | `grep -nE "\\bas any\\b\|: any\\b\|<any>"` across feature files | No match | PASS |
| B04 | No type assertions outside `as const` / `as unknown` | Read of all feature files; only `'ex-aequo' as const` in `ranking.core.ts:149` (preexisting pattern, allowed) | `idx === accumulator.previous?.index ? { ...entry, rank: 'ex-aequo' as const } : entry` | PASS |
| B05 | No abbreviations / single-letter locals in feature code | Read `fastest-lap.core.ts`: `candidates`, `minimum`, `seenSlugs`, `candidate`, `punch`, `duration`, `result` — all descriptive. `Leaderboard.tsx`: `decoratedSlugs`, `hasFastestLap`, `chipContent`, `entry`. `useStandingsPoll.ts`: `entry`, `listener`, `response`. | No 1-letter locals outside `for (let i…)` (which is also absent) | PASS |
| B06 | Magic numbers / strings named | CSS rule declares `--badge-size: 16px; --badge-offset: -6px;` at the rule top; back uses `60_000` (intervalMinutes → ms, repo-wide convention) directly in `loopDurationMs` | `apps/last-loop-lepin/api/src/punch/punch.core.ts:72`: `const intervalMs = edition.intervalMinutes * 60_000;` (named local) | PASS |
| B07 | Comments are WHY, not WHAT | `fastest-lap.core.ts` carries 2 JSDoc blocks (file-level + exported function) describing the contract & DNF-holder semantic; no inline what-comments. `leaderboard.css` carries a comment block explaining *why* `.leaderboard-chip` needs `position: relative` and *why* the inline `#a020f0` fallback exists. `useStandingsPoll.ts:62-65` explains the deploy-gap rationale for the normalisation. | All comments inspected — every one documents the WHY | PASS |
| B08 | JSDoc only on exported / shared | JSDoc lives on `fastestLap` (exported), `loopDurationMs` (exported), `lastLoopDurationMs` (exported), `Standings.fastestLap` (exported interface field), `StandingsDto.fastestLap` (exported interface field), `LeaderboardProps.fastestLapSlugs` (exported prop), `FastestLapBadge` (module-local — borderline; arguably internal but the doc explains the inline-SVG rationale, which is a WHY) | All JSDoc inspected; no internals-only doc on a non-exported helper | PASS |
| B09 | Function names describe the result | `fastestLap`, `loopDurationMs`, `lastLoopDurationMs`, `FastestLapBadge`, `formatLastEventTime`, `describeStatus` — all describe the value they produce, not the mechanism | (names read across feature files) | PASS |
| B10 | `noUncheckedIndexedAccess` honoured | The few array accesses in the diff: (a) `lastLoopDurationMs` uses `.reduce` to carry the last punch without indexing (commented `// sidesteps noUncheckedIndexedAccess`), (b) `ranking.core.ts:48` `sorted[sorted.length - 1]` uses optional-chaining downstream (`lastPunch?.finishedAt ?? null`), (c) `fastest-lap.core.test.ts:131` `[sorted[2], sorted[0], sorted[1]].filter((punch): punch is LoopPunch => punch !== undefined)` — explicit type guard | apps/last-loop-lepin/api/src/punch/punch.core.ts:88-94, apps/last-loop-lepin/api/src/ranking/ranking.core.ts:48-49 | PASS |
| B11 | `useEffect` is a smell | `grep -nE "useEffect" Leaderboard.tsx SpectatorPage.tsx useStandingsPoll.ts` returns one match — `useStandingsPoll.ts:6` is a comment line ("(b) component code stays free of `useEffect`"), not a call. No `useEffect(...)` invocation introduced. `useStandingsPoll.ts` uses `useSyncExternalStore`, which is the spec'd alternative for subscribing to a poll. | apps/last-loop-lepin/site/src/data/useStandingsPoll.ts:15 `import { useSyncExternalStore } from 'react';` | PASS |
| B12 | Knip clean | `pnpm exec knip` | exit 0 (no orphan exports / files / deps) | PASS |

## C. Tests pass

| # | Workspace | Command | Exit | Verdict |
|---|---|---|---|---|
| C01 | @borso-app/last-loop-lepin | `pnpm --filter @borso-app/last-loop-lepin run test:core` (coverage on) | 0 — 239 tests pass across 18 files; 100 % S/B/F on every `*.core.ts` and `*.utils.ts` in the diff (`fastest-lap.core.ts`, `punch.core.ts`, `ranking.core.ts` all green, `ranking.types.ts` 100 %, `*.utils.ts` files 100 %) | PASS |
| C02 | back-e2e suite | NOT RUN per TL constraint (shared local Postgres contention across 3 validator worktrees; implementer reported green) | — | UNVERIFIABLE |
| C03 | Every `*.utils.ts` in touched workspaces has a sibling test | `*.utils.ts` files in diff: `edition.schema.utils.ts`, `haversine.utils.ts` (api), `haversine.utils.ts` (site), `requestPosition.utils.ts`, `SelfPunchModal.utils.ts`, `course-map.utils.ts`. Every one has a `*.utils.test.ts` sibling and shows 100 % S/B/F coverage in the run | (coverage report) | PASS |

## D. Test coverage of spec

> Use cases routed to `/visual-validation` (out of scope for this report): the four UI badge assertions — badge appears on the seeded fastest runner's chip; double badge on a ms tie; no badge before the first loop close; badge retained on a DNF holder. Spec *Test strategy* line 137 names these as the visual-validation responsibility.

| # | Use case (in-scope, pure / DTO) | Covering test | Verdict |
|---|---|---|---|
| D01 | Happy path: `fastestLap` populated with the holder + durationMs after a few loops | `it('surfaces the fastest runner in fastestLap after a few loops, with the expected durationMs')` at apps/last-loop-lepin/api/src/ranking/ranking.core.test.ts:214-228 — asserts `[{ runnerSlug: 'bob', durationMs: 42 * 60_000 }]` | PASS |
| D02 | Edge: no loop closed → `fastestLap = []` | `it('returns [] when no punches are recorded')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:47-49 and `it('returns an empty fastestLap before any punch is recorded')` at apps/last-loop-lepin/api/src/ranking/ranking.core.test.ts:208-212 | PASS |
| D03 | Edge: single runner with a single loop → length 1 | `it('surfaces the only runner when a single runner has a single loop')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:59-63 | PASS |
| D04 | Edge: ms tie across distinct runners → length 2, both decorated | `it('returns both runners (length 2) when two distinct runners tie at the millisecond')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:80-90 | PASS |
| D05 | Edge: DNF holder retained (punches survive, fastestLap still surfaces them) | `it('keeps the record on a DNF holder (caller is responsible for filtering — we are not)')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:103-114 — confirms the projection never reads `ManualDnf` | PASS |
| D06 | Edge: all punches voided → `[]` | `it('returns [] when every punch is voided')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:51-57 | PASS |
| D07 | Edge: clock-skew boundary punch (`elapsed = 0`/`null`) ignored | `it('returns [] when every punch yields a null duration (clock-skew degenerate)')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:116-123 + `it('returns 0 when finishedAt equals the loop boundary exactly')` at apps/last-loop-lepin/api/src/punch/punch.core.test.ts:152-155 + `it('returns null when finishedAt precedes the loop boundary (clock skew)')` at apps/last-loop-lepin/api/src/punch/punch.core.test.ts:147-150 | PASS |
| D08 | Plan §G: same runner, two equal-min punches → dedup to length 1 | `it('dedupes by runnerSlug when one runner has multiple punches at the same minimum')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:92-101 | PASS |
| D09 | Plan §K: order-independence — `fastestLap` does not assume sorted input | `it('is order-independent — passing punches in arbitrary order yields the same record')` at apps/last-loop-lepin/api/src/ranking/fastest-lap.core.test.ts:125-135 | PASS |
| D10 | Plan §A: refactored `loopDurationMs` covers the elapsed branch directly | `it('returns the elapsed time from the loop boundary to finishedAt')` at apps/last-loop-lepin/api/src/punch/punch.core.test.ts:142-145 | PASS |
| D11 | Plan §D: client Zod tolerates server omitting `fastestLap` (deploy-gap absorbed); consumers always see `[]` | (no dedicated unit test; the plan flagged this as a manual self-check / Sentry-gated and the impl moved the normalisation to `useStandingsPoll.ts`). Verified by code inspection: `client.ts:75-77` `.optional()` + `useStandingsPoll.ts:65-72` `?? []`. No automated regression test was added — a server response omitting `fastestLap` will parse cleanly and downstream `standings.fastestLap` will be `[]`, but no unit test asserts this end-to-end | UNVERIFIABLE |

## Notes

- **C02** — back-e2e suite (`pnpm --filter @borso-app/last-loop-lepin run test`) intentionally not executed. TL brief constrains: shared local Postgres across 3 validator worktrees would race. Implementer reported the suite green. The plan §E risk row mentions adding an assertion in `ranking.controller.test.ts` that the body has `fastestLap`; `ranking.controller.test.ts` is marked `M` in the diff but its content is part of the back-e2e suite, so this report cannot confirm that specific assertion runs. Acceptable per TL constraint, surfaced for the operator.
- **D11** — `client.ts` Zod schema uses `.optional()` (not `.optional().default([])` as the plan originally specified). The implementer moved the normalisation to `useStandingsPoll.ts:65-72` where `response.standings.fastestLap ?? []` pins the value. Behaviour is equivalent (server omits → client `StandingsDto.fastestLap === []`). The deviation is documented inline in the Zod comment block (lines 70-74). The only gap is that no automated unit test asserts the round-trip; the plan's "Add a unit-ish smoke … parsing a known fixture without `fastestLap`" line was not realised. Low-impact — TypeScript's non-optional `StandingsDto.fastestLap` plus the explicit `?? []` makes the regression surface essentially impossible without typecheck noticing.

## Verdict: PASS_EXCEPT_UNVERIFIABLE
