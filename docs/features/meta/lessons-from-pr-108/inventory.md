# Friction inventory — PR #108

Every friction event during the reproduction and fix of the pragma blank
screen, in the order it happened. The decision column is the classification;
the entries it names are in [`docs/dantotsus/`](../../../dantotsus/) and
[`docs/knowledge/`](../../../knowledge/).

`KAIZEN.md` carried 6 entries, all from `main` plus two hook-written ones. The
task ran no subagents, so there is no per-agent grouping to read and the log is
complete rather than partial. It is archived at
[`docs/features/pragma/blank-screen-after-deploy/kaizen.md`](../../pragma/blank-screen-after-deploy/kaizen.md).

| # | When | Friction | Sources / evidence | Decision |
| --- | --- | --- | --- | --- |
| 01 | implementation | `sw.js` was eslint-ignored through `apps/*/site/public/**`, so the file mediating every request for every user was the only unchecked source in the tree | `kaizen:08:56`, `commit:fe6c44c` | `no-op: eradicated inside PR #108 by deleting the folder ignore; sw.js was the only lintable file under any public/` |
| 02 | implementation | `sw.js` and `sw/sw-cache.utils.ts` hold two hand-copied versions of the same predicates and no gate makes them agree | `kaizen:08:56`, `scripts/check-pure-modules-have-callers.sh` allow-list | `dantotsu: the-allowance-that-let-a-duplicate-grow` |
| 03 | implementation | the allow-list entry parking that duplication said "consolidating needs a SW bundling decision", and PR #108 added a third predicate to the parked pair with every gate green | `commit:0b560c3`, allow-list reason string | `merge into the-allowance-that-let-a-duplicate-grow` |
| 04 | implementation | `sw/manifest.utils.ts` is a third implementation of the offline manifest; the live one is `api/src/sessions/offline-manifest.core.ts` and `sw.js` holds a fourth in `listManifestUrls` | `grep`: only importer is its own test | `merge into the-allowance-that-let-a-duplicate-grow` |
| 05 | validation | a test written at `site/src/sw/service-worker.test.ts` was collected by no project; vitest answered "No test files found" and printed the four suffix-keyed includes | vitest output, this session | `dantotsu: the-test-that-no-project-collected` |
| 06 | validation | the two full-stack workspaces collect site tests by gated suffix while the two front-end-only ones collect everything, so the same file runs in `borso-fr` and silently not in `pragma` | `apps/*/vitest.config.ts` compared | `merge into the-test-that-no-project-collected` |
| 07 | deploy | the prod static site shipped with no `Cache-Control` on any object, so browsers applied heuristic freshness to `index.html`; nothing in the repo asserts a response header | `kaizen:08:56`, `curl -D-` against pragma.borso.fr | `no-op: eradicated inside PR #108; the synth assertions in static-site.test.ts now pin both header sets` |
| 08 | deploy | the prod `BucketDeployment` pruned, so every deploy deleted the bundles a live client still needed | `infra/cdk/src/constructs/static-site.ts:161` before the fix | `no-op: eradicated inside PR #108 by prune: false on both deployments` |
| 09 | deploy | the SPA fallback answers a missing `/assets/*.js` with `index.html` under status 200, so a module script arrives as `text/html` | `curl` on a bogus asset path, 200 + text/html | `no-op: eradicated inside PR #108 by the content-type guard in cacheFirst; the fallback itself is CloudFront-wide and correct for navigations` |
| 10 | deploy | previews carry the service worker fix but not the cache headers, because the change is in `buildProd` and previews run `buildPreview`, so CI cannot observe half the fix | live check on `pragma-pr-108.preview.borso.fr` | `no-op: surfaced to the operator on the PR; previews already set prune: false so the failure chain never closes there, and changing it is a call about preview parity, not a defect` |
| 11 | validation | a filtered `vitest run --project core --coverage <path>` fails with 0% on every unrelated gated file, which reads as a broken suite rather than as a filtered run | `kaizen:08:56`, run output | `no-op: inherent to a per-file coverage threshold over a filtered selection; the repo already runs the full suite in test:core and pre-push scopes by changed files` |
| 12 | validation | two Stryker mutants survived on `recovery.adapter.ts`, because two `in` guards inside one shared try/catch made no observable difference | pre-push gate output, `commit:606f10a` | `knowledge: writing-a-small-function-the-hundred-percent-gates-accept` |
| 13 | validation | a named helper `ignoreRefusal(): undefined { return undefined }` is an equivalent mutant no test can kill | Stryker output after the first fix | `merge into writing-a-small-function-the-hundred-percent-gates-accept` |
| 14 | implementation | `noUncheckedIndexedAccess` forced a guard on `split(';')[0]` that no test can reach, failing the 100% branch gate at 92.85% | `test:core` coverage output | `merge into writing-a-small-function-the-hundred-percent-gates-accept` |
| 15 | implementation | the first i18n insertion re-serialised the catalogue and produced 68 changed lines for a 6-line addition, because `audience` sat outside alphabetical order | `git diff --stat` before the revert | `knowledge: inserting-an-i18n-key-without-rewriting-the-catalogue` |
| 16 | validation | `scripts/browser.sh open http://localhost:8099/` failed with `net::ERR_ABORTED` and read as a proxy problem; the repro server was answering `/` as `application/octet-stream` | session transcript, `curl -D-` on the same URL | `knowledge: agent-browser-aborts-a-navigation-served-as-octet-stream` |
| 17 | validation | stripping `HTTP_PROXY` and setting `NO_PROXY` changed nothing, which read as a failed fix rather than a ruled-out cause | session transcript | `merge into agent-browser-aborts-a-navigation-served-as-octet-stream` |
| 18 | pr-description | `pull_request_read get_files` on PR #107 returned 252,876 characters and was refused; the first recovery attempt decoded it with `unicode_escape` and crashed on a backslash | tool error, `UnicodeDecodeError` | `knowledge: reading-a-large-pull-request-through-the-github-mcp` |
| 19 | validation | the harness blocked `pkill -f server.mjs` on a shared machine | `kaizen:08:38`, `hook:no-broad-kill` | `no-op: the hook did its job and named the alternative; the friction is the cost of a correct refusal` |
| 20 | validation | `ss` is not installed in the sandbox, so the hook's suggested `ss -lptn` recovery does not run; `fuser -n tcp <port>` does | `bash: ss: command not found` | `no-op: worth a line in the hook's message rather than an entry, and the hook already offers the PID-capture path that avoids the lookup entirely` |
| 21 | pr-description | `git push` piped into `tail` was refused because the pipeline discards the push's exit status | `kaizen:08:57`, `hook:no-swallowed-push` | `no-op: the hook did its job and printed the pipefail form that works` |
| 22 | pr-description | the first PR body draft broke 6 of the budget's limits at once | `scripts/pr/check-pr-body.ts` output | `no-op: the checker is the eradication and it worked; writing over budget then trimming is the intended loop` |
| 23 | conception | `/route` was never run, although CLAUDE.md asks for it on anything that is not obviously one file, and this touched 14 files across two workspaces | session transcript | `no-op: CLAUDE.md already states it; a second statement is not an eradication and a hook that watched for it would fire on every session` |
| 24 | validation | `/standards-review` was not run before opening the PR, so nothing on the branch is sealed | `docs/standards/seals.jsonl` unchanged | `no-op: the skill's own README says it is not wired into CI yet and is run by hand; the gap is known and recorded there` |
| 25 | post-merge | five bot comments on the PR each woke the session for a preview URL or a map diff, costing a turn apiece to read and dismiss | four `issue_comment.created` events on #108 | `no-op: the wakes are how a subscription works and each was correctly dismissed in one call; suppressing them would suppress the review comments too` |
